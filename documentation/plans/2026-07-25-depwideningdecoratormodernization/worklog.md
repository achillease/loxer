# Worklog — 2026-07-25-depwideningdecoratormodernization

> **How to log:** read this file first, then add your row at the **bottom** of the table, below the last existing row (newest last — never insert after the header). The **Timestamp** is `YYYY-MM-DD HH:MM` from a real clock (`Get-Date -Format "yyyy-MM-dd HH:mm"` / `date "+%Y-%m-%d %H:%M"`) — never guess the time. Prefix each **Action** with the workflow phase in brackets — e.g. `[Planning]`, `[Implementation]`, `[Testing]`, `[Reviewing]`, `[Documentation]`, `[Finalization]` — so every entry is traceable to the phase that wrote it.

| Timestamp | Action | Detail |
|-----------|--------|--------|
| 2026-07-25 22:04 | [Planning] | Wrote the plan to widen the plugin peer ranges (Babel 7, Vite 5–8, Node floor) and make `@trace` dual-mode. Grounded by an architect technical survey and a web-researcher version-fit gate. The gate overturned four premises: Babel floor moved `^7.23.0` → `^7.26.10` (7.23.0/.1 carry CVE-2023-45133), `engines.node` `>=20` → `>=20.19.0` (Babel 8 needs ≥22.18, Vite 7/8 need ≥20.19), `assertVersion` has accepted range strings since 7.0.0 not 7.13, and `@vitejs/plugin-react@6` no longer depends on `@babel/core` at all. Survey found two implementation blockers the draft missed: root `package.json` has no `vite` devDep (so a root test cannot `import from 'vite'`), and `vite@5`'s esbuild postinstall needs `onlyBuiltDependencies` or it is silently skipped with exit 0. Also found the real Babel type leak (`export type BabelTypes = typeof import('@babel/core')` in the shipped `.d.ts`), a latent highlight-loss bug in the decorator's async close path, and the bare-`@initLoxer` footgun the dual-mode change would create. Reverses `2026-07-22-babelplainfunctiontracing/plan.md:25-26`. Open decision recorded: Vite 5.4 is EOL with an unpatched advisory, kept in range per instruction. Plan folder is under `documentation/plans/` not `seed-plan.js`'s `docs/plans/` default, per `rules/documentation.md`. |
| 2026-07-25 23:39 | [Implementation] | Completed workstream T by removing all 11 vestigial `// @ts-ignore` comments from `test/decorators.test.ts`; no source behavior or assertions changed. Verified zero remaining `@ts-ignore` comments, `pnpm typecheck:test`, `pnpm lint`, `pnpm test` (132 tests), and `pnpm build` all pass. |
| 2026-07-25 23:47 | [Implementation] | Paused workstream A after implementing the manifest ranges, structural Babel type bridge, esbuild build allowlist, lockfile regeneration, and the planned full-audit CI step. `pnpm install` ran esbuild's postinstall with no ignored-build-script warning; build and lint passed, and emitted declarations contain no `@babel/core` reference. The full `pnpm audit` exits 1 with five dev-tree advisories, including the known unpatched Vite 5.4 high-severity advisory plus an unrelated ESLint transitive advisory, so an unconditional audit step would make every CI matrix job fail. This invalidates the plan's audit mitigation and requires a decision on Vite 5 support/audit policy before implementation can continue. |
| 2026-07-26 00:02 | [Implementation] | Replaced the provisional hand-written Babel type replicas after user review. Added `@babel/types` as a direct development dependency, restored real Babel `NodePath` and builder typings internally, marked the cross-module trace-binding declaration internal with `stripInternal`, changed the source-map boundary to `unknown`, and retained only one deliberate `any` at the exported cross-major plugin-factory return. `types.ts` now contains zero `any`s; emitted declarations reference neither `@babel/core` nor `@babel/types`. Build, lint, test typecheck, and all 132 existing tests pass. Workstream A remains paused on the audit-policy re-plan. |
| 2026-07-26 00:09 | [Implementation] | Completed workstream B's implementation surface. `@trace` now dispatches between legacy and standard method-decorator protocols, returns the protocol-appropriate replacement/descriptor, rejects non-method use with `TypeError`, resolves string/symbol method names and class names at call time, preserves rejection behavior, and hoists async close formatting before the Loxer modifier chain. `initLoxer` still initializes eagerly and now returns a no-op decorator for either protocol while guarding bare `@initLoxer`. Added the four public protocol types without referencing TypeScript 5 decorator globals; all helpers remain private. Build, lint, test typecheck, the 11-test focused legacy decorator suite, all 132 existing tests, and a direct standard-protocol runtime smoke pass. New cross-protocol/standard-emit tests and user-facing documentation remain deferred to their workflow phases. |
| 2026-07-26 00:19 | [Testing] | Added compatibility and protocol coverage across workstreams A and B: Babel 7 transform/runtime tests, real Vite 5 and Vite 8 plugin-container builds over a committed fixture, a shared 12-case legacy/standard decorator matrix, call-time class/symbol/detached behavior, async formatter highlight preservation, decorator/init protocol contracts, and real TypeScript standard emit plus dual-mode type conformance. No product bugs were revealed and no `test-bugs.md` was needed. `pnpm test` passes 163 tests across 13 files; `pnpm typecheck:test`, `pnpm build`, and `pnpm lint` also pass. |
| 2026-07-26 00:29 | [Reviewing] | Review pass 1 completed with WARN; see `review.md`. Five lenses found two HIGH issues (a guaranteed-failing full-tree audit in every CI matrix leg and an accidental narrowing of the transform helper's public result type) plus four MEDIUM test gaps covering module/level assertions, rejection identity/error silence, `Class` suffix trimming, and production callback silence. Accessibility was skipped because the change has no UI. |
| 2026-07-26 00:33 | [Implementation] | Fixed review pass 1's two HIGH findings. The full development-tree audit now runs once on Ubuntu/Node 24 and is explicitly informational while the existing production audit remains blocking; this preserves visibility without making all nine matrix legs fail or repeat the same registry request. Restored the transform helper's prior public result surface with Babel-neutral structural fields while keeping Babel type packages out of emitted declarations. |
| 2026-07-26 00:33 | [Testing] | Fixed review pass 1's four test gaps. Protocol records now compare observable module and level values with a configured visible level-2 module, rejection tests preserve sentinel identity and assert error silence, both protocols cover `Class`-suffix shortening, and every parity run asserts production callbacks remain empty. The first focused run corrected `NONE` expectations to Loxer's observable `DEFAULT` normalization; focused tests then passed 36/36. Full verification passes 163 tests plus test typecheck, build, lint, declaration-dependency, and diff checks. |
| 2026-07-26 00:36 | [Reviewing] | Review pass 2 completed with PASS; see `review-2.md` (pass 1 retained in `review.md`). Code, security, performance, acceptance, and test lenses confirmed both HIGH blockers and all four MEDIUM test gaps are fixed. One LOW documented-rule exception remains for the deliberately small Babel-neutral `any` compatibility boundary; coverage gaps are deferred to Documentation. Accessibility remained out of scope because the change has no UI. |
| 2026-07-26 02:07 | [Documentation] | Learned the user-approved Babel typing boundary in `packages/babel-plugin-loxer-trace/AGENTS.md`: use authoritative Babel development typings rather than hand-rolled facsimiles, keep published declarations consumer-neutral, preserve the complete public result surface in compatibility types, and permit explicit `any` only at that narrow dependency-neutral boundary. The sibling `CLAUDE.md` continues to import the canonical `AGENTS.md`. |
| 2026-07-26 02:10 | [Finalization] Commit | [2026-07-25-depwideningdecoratormodernization] Modernize dependency compatibility and decorators |
