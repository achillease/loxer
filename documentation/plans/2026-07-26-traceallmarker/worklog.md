# Worklog — 2026-07-26-traceallmarker

> **How to log:** read this file first, then add your row at the **bottom** of the table, below the last existing row (newest last — never insert after the header). The **Timestamp** is `YYYY-MM-DD HH:MM` from a real clock (`Get-Date -Format "yyyy-MM-dd HH:mm"` / `date "+%Y-%m-%d %H:%M"`) — never guess the time. Prefix each **Action** with the workflow phase in brackets — e.g. `[Planning]`, `[Implementation]`, `[Testing]`, `[Reviewing]`, `[Documentation]`, `[Finalization]` — so every entry is traceable to the phase that wrote it.

| Timestamp | Action | Detail |
|-----------|--------|--------|
| 2026-07-26 15:52 | [Planning] | Wrote `plan.md` for `traceAll(targets, options)` after answering a question about `trace()` marker placement. Answering it exposed a latent defect: the plugin declared each marker's hoisted options storage before the *target's* declaration statement, so a marker placed **above** its declaration had its options silently overwritten by the declaration's `= {}` initializer. A shared options variable for several targets cannot use a per-target insertion point anyway, so one module-scope declaration per marker fixes both. No spec or grounding agents — direct user request. |
| 2026-07-26 15:55 | [Implementation] | Added the `traceAll` runtime marker (`src/trace.ts`) with union-inferring formatter types, and reworked the plugin to carry N targets per marker: marker bindings are now a `Map<binding, 'trace' \| 'traceAll'>`, `collectTargets`/`resolveTarget` validate an array literal of resolvable identifiers, `assertOneMarkerPerTarget` runs before any codegen, and `declareTraceOptions` puts one hoisted `var` per marker at module scope before the first non-import statement. `trace-binding.ts` needed no change. Verified generated output for twelve placement/validation fixtures via a scratch driver (`transformLoxerTrace`), including nested markers, aliased imports, and each diagnostic. |
| 2026-07-26 15:56 | [Testing] | Extended `test/plain-function-trace.test.ts` with four cases (shared options evaluated once across declaration/async/arrow targets with three distinct box IDs and one linked inner log; marker above its declarations still applying options; `traceAll` and `trace` coexisting with separate options; eight rejected marker forms) and added a Babel 7 `traceAll` case to `test/babel7-compat.test.ts`. Full suite passes 168 tests across 13 files, plus `pnpm typecheck:test`, `pnpm lint`, and `pnpm build`. |
| 2026-07-26 15:59 | [Documentation] | Documented the (now removed) `traceAll` in `documentation/index.md` (new "Tracing several functions with the same options" section covering shared single-evaluation options, array-literal-only targets, one-marker-per-target, and union-typed formatters), the root `README.md`, and the plugin `README.md` (usage plus the boundaries list). Updated `TraceOptions` JSDoc in `src/tracing-types.ts` for the second marker and regenerated `docs/` with `typedoc --options typedoc.json` (exit 0, 25 pre-existing warnings; the run also picked up a stale missing `Decorators.InitLoxerClassContext.html`). Note: `src/trace.ts` is not a `typedoc.json` entry point, so neither `trace` nor `traceAll` appears in the generated API reference — pre-existing gap, left as is. Also ran `pnpm demo:build` to exercise the real Vite adapter path. |
| 2026-07-26 16:14 | [Implementation] | User changed the design: `trace()` itself now takes a single target **or** an array literal of targets, and `traceAll` is removed. Widened the marker's first parameter to `T \| readonly T[]` in one signature (no overloads), dropped `traceAll` from `src/trace.ts`, and collapsed the plugin back to one marker binding — `markerBindings` is a `Set` again, `MarkerName` and the marker-specific diagnostics are gone, and `collectTargets` branches on whether the first argument is an array literal. Every diagnostic keeps its pre-existing `trace()` wording, so no Babel 7 code-frame assertion changed. Shared options storage is named `_sharedTraceOptions` only when a marker has more than one target, otherwise it keeps `_<target>TraceOptions`. |
| 2026-07-26 16:14 | [Testing] | Rewrote the list coverage onto `trace([...])` in both suites and replaced the dropped "expects an array literal" case with a variable-holding-an-array case, which now fails as an unsupported target rather than a bad argument shape. Type fixtures cover both call forms, so `pnpm typecheck:test` pins that a single target still infers its exact argument tuple while a list infers the union. 169 tests pass, plus lint, build, test typecheck, TypeDoc, and a re-verified codegen sweep over six placement fixtures (single, list, single-element list, aliased/nested marker, array-valued variable, unrelated local `trace` function). |
| 2026-07-26 17:13 | [Testing] | Ran the Testing phase via `write-tests`. Dispatched `test-author` for code-level coverage of the list form; it added 8 integration tests and 2 compile-time type fixtures to `test/plain-function-trace.test.ts` (mixed callable forms and `this`/`arguments`/arity in one list, self-recursive member re-entering its own box, one member's throw not disturbing siblings, native Promise identity beside a sync sibling, interleaved concurrent members with no box cross-talk, uniform `argsAsItem`/`resultAsItem`/`level`/`highlight`, nested-scope marker re-evaluation, shadowed `Array`/`Object` around a list group, plus `as const` and mixed-signature formatter type fixtures), one transform-level list case to `test/vite-plugin-loxer-trace.test.ts`, and a real Vite 5 + Vite 8 plugin-container build over the new `test/fixtures/vite-app-list/` app. E2E was skipped: the change is a build-time marker with no user-facing UI. Suite is 180 tests across 13 files, with `pnpm typecheck:test`, `pnpm lint`, and `pnpm build` all green. Verified the agent left product code untouched and unstaged the index it had staged. |
| 2026-07-26 17:13 | [Testing] | Found one product bug during review of the new tests and persisted it to `test-bugs.md`: `declareTraceOptions` puts every marker's options `var` at **module scope**, so a marker written inside a function that runs more than once shares one options slot and the last invocation's options win for all earlier closures. This is a regression from the pre-change placement (before the target's own declaration statement, hence per-invocation). Confirmed against emitted code for both the single and list forms. Left unfixed per Testing-phase rules — a fix proposal (declare in the outermost target scope, and drop the `= {}` initializer so hoisting keeps the marker-above-declaration case working) is with the user. Also corrected the misleading name of the agent's nested-scope test, which claimed per-invocation isolation its identical-content options could not observe. |
| 2026-07-26 17:19 | [Implementation] | Fixed `test-bugs.md` finding 1 on user instruction. `declareTraceOptions` now declares an **uninitialized** `var` in the outermost of the marker's target scopes (`outermostTargetScope` + Babel's `scope.push({ kind: 'var' })`) instead of one initialized slot at module scope, so a marker inside a re-invoked function gets fresh storage per invocation while a target in an outer scope stays reachable; dropping the initializer keeps the marker-above-declaration case working through `var` hoisting alone. Added the regression test the bug report specified (single and list form, options computed from the enclosing parameter, first group asserted to keep its own `moduleId` after a second setup) and re-verified emitted code across five scope shapes, including two markers in one scope merging into `var a, b;` and a block-scoped target. Updated the plan's step 3, its risk note, and the plugin README's transform description. 181 tests pass, plus `pnpm typecheck:test`, `pnpm lint`, `pnpm build`, and `pnpm demo:build`. |
| 2026-07-26 17:24 | [Documentation] | Ran a `learn` sweep. One lesson landed, to memory rather than repo docs: never modify the git index as cleanup (`git-index-hands-off`), after I unstaged the user's deliberate staging on the assumption that a subagent's `git add -A` had caused it. Two candidates were proposed and declined by the user: an `AGENTS.md` invariant for the package about generated per-marker storage (outermost target scope, no initializer) and a coding-conventions rule preferring a widened marker over a parallel export. Dropped before proposal: the misleading-test-name catch (my own review finding, no user reaction) and the plan-folder location fix (already in memory). No repo docs changed by this phase. |
| 2026-07-26 17:38 | [Reviewing] | Ran `run-reviews` pass 1 → **WARN**, written to `review.md`. Four lenses dispatched in parallel (code, perf, acceptance, test); security skipped (no manifest/lockfile change, no security-relevant surface) and a11y skipped (no user-facing UI). Code, perf, and acceptance came back clean; all ten acceptance criteria met, with `pnpm test`/`pnpm docs`/`pnpm demo:build` left unverified because the phase is read-only. Three findings, all in the tests: 1 HIGH — the outermost-target-scope invariant is regression-guarded only by a throwaway scratch driver for a list spanning two scopes, two markers in one scope, and a block-scoped target (the code lens independently disproved both failure paths against `@babel/traverse` source, so the residual risk is a missing guard, not a suspected defect); 2 MEDIUM — the union-inference type fixture uses a one-directional assignment that cannot catch inference collapsing to one branch, and the new array-literal parsing path has no array-hole (`trace([a, , b])`) case. Six rule-coverage gaps recorded for the Documentation phase, headed by the absence of any spec for this change. Advisory only: no product code, tests, or docs touched, and the git index left exactly as staged. |
| 2026-07-26 17:50 | [Implementation] | Fixed all three `review.md` findings, tests only — no product code touched (verified `git diff` against the index is empty for `src/` and `packages/`). HIGH: added the three missing scope-shape regression tests (`a list marker shares one options slot across targets declared in two nested scopes`, `two list markers in one nested scope get separate per-invocation options slots`, `a marker on a block-scoped target keeps per-invocation options`), then **proved they bite** by temporarily reverting `declareTraceOptions` to the pre-fix `getProgramParent().push(...)` placement — all three failed alongside the existing 866 regression test, and the fix was restored byte-identically. The two-list-marker case also pins that two list markers in one scope get distinct uids (`_sharedTraceOptions`/`_sharedTraceOptions2`) merged into a single `var` statement. MEDIUM: replaced the one-directional `const x: Expected = args` type pins in all four formatter fixtures with a bidirectional `Equals` helper (mutual assignability plus an `IsAny` guard, so a collapsed union branch or a widening to `any` now fails `pnpm typecheck:test`). Doing so surfaced that the `as const` list form infers `Parameters<T>` as an unreduced `[id: string] \| [id: string]`, which is why the helper normalizes duplicate union members rather than checking type identity. MEDIUM: added the array-hole case `trace([one, , two])`, and while in that block closed two pre-existing coverage gaps `review.md` named — an anonymous single target and `trace() options cannot be a spread argument.` (previously untested anywhere). 184 tests pass (was 181), plus `pnpm lint`, `pnpm build`, `pnpm typecheck:test`, and a prettier check on the edited suite. |
| 2026-07-26 18:06 | [Finalization] Commit | [2026-07-26-traceallmarker] Support shared trace target lists |
