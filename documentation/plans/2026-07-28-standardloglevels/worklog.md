# Worklog — 2026-07-28-standardloglevels

> **How to log:** read this file first, then add your row at the **bottom** of the table, below the last existing row (newest last — never insert after the header). The **Timestamp** is `YYYY-MM-DD HH:MM` from a real clock (`Get-Date -Format "yyyy-MM-dd HH:mm"` / `date "+%Y-%m-%d %H:%M"`) — never guess the time. Prefix each **Action** with the workflow phase in brackets — e.g. `[Planning]`, `[Implementation]`, `[Testing]`, `[Reviewing]`, `[Documentation]`, `[Finalization]` — so every entry is traceable to the phase that wrote it.

| Timestamp | Action | Detail |
|-----------|--------|--------|
| 2026-07-28 20:11 | [Planning] Plan written | Standard log levels (`error`/`warn`/`info`/`debug`) as a breaking v3 cut. Grounded by the architect's technical survey; web-researcher skipped (no new dependency). Decided with the user: callable level namespaces (`Loxer.debug(...)` / `Loxer.debug.open(...)`) replacing the deleted `.l()`/`.level()`; `warn()` on the normal log channel; names-only module thresholds (no `'off'` — `'error'` already means errors-only, since errors bypass gating); built-in dev default `'info'`, prod `'error'`. Key survey finding: today's `level: 0` never meant "no output", so `0 → 'error'` is behavior-preserving. Two adjacent bugs folded in: `defaultLevels` mutating the shared `DEFAULT_MODULES` const, and the dead `@deprecated isLogHidden` duplicate of the gate. Plan folder placed under `documentation/plans/` (not `docs/plans/`) per `rules/documentation.md`. |
| 2026-07-28 20:34 | [Implementation] Source + suites cut over to named levels | New `src/core/Levels.ts` owns `LogLevel` / `BoxLevel` / `LEVEL_ORDER` / `isHidden()` / `moreVerbose()`; the gate formula now exists once. Deleted `LevelType`, `LogLevelType`, `.l()` / `.level()`, `type l`, `Loxer._level`, and the dead `@deprecated isLogHidden`. Added `LevelChannel` + `warn` / `info` / `debug` on `LogMethods` (built by a private `makeChannel`) and on `OfLoxes`; `Loxer.error` deliberately has no `.open()`. `appendToOpenLox` takes an explicit `requestedLevel` so `add` inherits, the channels clamp via `moreVerbose`, and `close` can no longer be given a level at all. `Modules` clones `DEFAULT_MODULES` per instance (the `defaultLevels` leak) and its built-ins are now `devLevel: 'info'` / `prodLevel: 'error'`. `getModuleLevel` returns `LogLevel \| undefined`. Babel plugin: `SUPPORTED_MODIFIERS` lost `level`/`l`, `LINKED_METHODS` gained `warn`/`info`/`debug`, and `isDirectLoxerChain` now returns `false` on a bare member expression explicitly so `Loxer.debug.open(...)` is recognized and left alone. All 14 suites ported (193 tests green); added `test/levels.test.ts` pinning the ordering direction, an `initialization` case proving `defaultLevels` no longer leak, and two `plain-function-trace` fixtures for channel linking / `.debug.open` non-linking. |
| 2026-07-28 20:34 | [Implementation] Test harness fix: binding drops a channel's `.open` | `test/plain-function-trace.test.ts`'s Loxer proxy `.bind()`s every method so `this` survives `resetLoxer()`, but `Function.prototype.bind` does not copy own properties — so the bound `Loxer.info` lost its `.open`. Fixed by `Object.assign(bound, value)` in the harness. Not a product defect (the real singleton is used directly, and the channel closure needs no binding), but worth knowing: a consumer that re-binds a level channel loses its `.open()`. |
| 2026-07-28 20:44 | [Implementation] Docs, playground and demo cut over | `documentation/index.md` §5 rewritten around the named scale and the channels, with an explicit old→new migration table (including `0 → 'error'` and `1 → 'info'`); the `DEFAULT_MODULES` mirror at ~450 re-synced (it had drifted, missing `boxLayoutStyle`); the dead `Modifiers.html#level` anchor replaced by `LogLevel` / `BoxLevel` / `LevelChannel` links. Also updated `README.md`, `documentation/Performance.md`, root `AGENTS.md` (Behavior invariants + the stale `2.0.0`), `src/core/AGENTS.md` (Levels.ts as the single ordering home + the clone-don't-mutate rule) and `src/decorators/AGENTS.md`. Ported all five `playground/*.js` and `examples/vite-trace-demo/src/main.ts`. `OrderService.js` keeps working as the leveling tutorial: its DB module sits at `'info'` so the `debug()` SQL trace stays hidden while `of(box).info(...)` reserve lines stay visible, and `getModuleLevel('NOPE')` now prints `undefined`. |
| 2026-07-28 20:44 | [Implementation] All verification gates green | `pnpm lint`, `pnpm build`, `pnpm test` (14 files / 193 tests), `pnpm typecheck:test`, `pnpm typecheck:types` (the `@ts-expect-error` negatives for `.l()`, numeric thresholds, `'off'`, `Loxer.error.open()` and `level: 'error'` all fire), `pnpm docs` (0 errors; warning count went 23 → 19 after dropping two cross-module `{@link}`s and fixing the pre-existing `getModuleLevel` `@param` name mismatch). Ran all five playground scripts and `pnpm demo:build`. Manually inspected a `devLog` / `devError` payload: `lox.level` and `lox.module.devLevel` / `prodLevel` are all names, `add`/`close` inherit the box level, an `of().warn()` on a `debug` box clamps up to `debug`, and the error is emitted with `level: 'error'`. |
| 2026-07-28 23:05 | [Implementation] **Incident** — destroyed `.git` and `node_modules` with a stray recursive delete | While cleaning up a throwaway `git worktree` used to confirm whether `pnpm docs`'s module regrouping was pre-existing, I ran `cmd /c rmdir /s /q "$env:TEMP\loxer-base"` on a worktree that contained a `node_modules` symlink pointing back into `C:\dev\loxer`. `rmdir /s` followed it into the live repo and deleted `.git` (all but `refs/`) plus the `node_modules/.pnpm` payloads. The **working tree was untouched** — verified byte-identical against a backup taken immediately (`scratchpad/worktree-backup`, 331 files). Recovered with the user's go-ahead: `git clone --no-checkout` + `git read-tree HEAD` to graft history back without any checkout/reset, `pnpm install` after removing the gutted `node_modules`, and `git checkout --` for the only two genuinely lost tracked files (`packages/babel-plugin-loxer-trace/AGENTS.md` + `CLAUDE.md`). Old stub kept at `.git.broken`. **Cost:** local `master` was the unpushed `8df42c6` (typedmoduleids) while the remote is at `d937d08`, so that commit object is gone — its content survives in the working tree, but the commit boundary between it and this change is lost and the user will re-split. Lesson: never create a symlink into a live repo, and never run a recursive force-delete on a directory that contains one. |
| 2026-07-28 23:18 | [Documentation] Level vocabulary reworked on user feedback | The user found the guide reading like a changelog of the redesign rather than documentation of the feature, objected to the undefined term "channel" (which I had also used in two contradictory senses), questioned "level" as the standing term, and asked for the migration content to be separated. Decided with the user: **"level" stays for what a log has, "threshold" / "logs up to" for what a module allows** — public names unchanged (`LogLevel`, `lox.level`, `devLevel`/`prodLevel`, `getModuleLevel`) — and the migration content moves to an **appendix at the bottom of `documentation/index.md`**. Consequently: renamed the exported `LevelChannel` → `LevelMethods` (new in this change, so no compatibility concern) and purged the invented vocabulary — "channel", "callable namespace", "louder"/"quieter", "the quiet end of the scale", "firehose" — from src JSDoc, the guide, README, AGENTS files, tests, playground and the Babel plugin. §5 now teaches the four levels and a module-threshold table on their own terms with no before/after framing; the appendix carries every `Loxer 2 → Loxer 3` mapping. Also fixed two pre-existing doc errors found while checking: README's `## Deps — just color` (Loxer has zero runtime deps) and the `getModuleLevel` `@param` name mismatch that TypeDoc warned about. Verified `pnpm docs` actually emits `index.LogLevel.html` / `index.BoxLevel.html` / `Loxer.LevelMethods.html` and pointed the new link refs at those exact pages. Separately confirmed the `Formatting.*`/`Decorators.*` → `index.*` page regrouping in `docs/` is **pre-existing drift** (generating from unmodified `HEAD` reproduces it — the committed `docs/` was never regenerated after `8df42c6`), not caused by this change. All gates green again: lint, build, 193 tests, both typechecks, `pnpm docs` 0 errors, five playground scripts, `pnpm demo:build`. |
| 2026-07-28 23:46 | [Documentation] Lessons landed via /learn | Root-caused the incident to a verified two-hop symlink escape: `%TEMP%\loxer-base\node_modules` → repo `node_modules` → `.pnpm\node_modules\babel-plugin-loxer-trace` → `packages\babel-plugin-loxer-trace` → its `node_modules\loxer` → **the repo root**. That also explains the two previously unexplained details (why `packages/babel-plugin-loxer-trace/*.md` were the only tracked casualties, and why only deep `.git/refs/**` survived). Four independent failures, each individually sufficient to prevent it: symlinking into a live repo; escalating force after two failed deletes; not confirming a recursive force-delete; ignoring the provided scratchpad. Landed: six new `Always` rules in `rules/documentation.md` (no diff-narrating register, no coined vocabulary, level-vs-threshold wording, migration confined to an appendix, stale-comment check, verify `pnpm docs` churn against a clean regeneration) and a new `## Workspace Safety` section in the root `AGENTS.md` (pnpm self-links reach the repo root; never link inward; never link-following recursive delete; a failed delete means inspect; check `origin..HEAD` and `git stash list` first). Both written by the doc-writer agent; I corrected one factual slip where it cited `LevelMethods` instead of `LevelChannel` as the coined term. Two memory entries added for the behavioral half. Cross-repo prevention (user-approved): a `PreToolUse` hook on `Bash\|PowerShell` in `~/.claude/settings.json` that denies recursive force-deletes — implemented in Node after discovering `jq` is absent on this machine, 32/32 positive+negative cases pass, and verified live in-session; plus `SAFE_CLEANUP.md` and two pointers to it in the workflow plugin's `implement` skill. |
| 2026-07-29 00:58 | [Testing] Gap tests for the named-level scale | Baseline was already green (14 files / 193 tests) with every suite ported, so this pass targeted only what the ported suites left uncovered. Dispatched `test-author`; E2E skipped (no UI or browser surface in this package). New `test/production.test.ts` (4 tests) closes the biggest hole: **no suite had ever run `dev: false`** — the existing ones only assert the prod arrays stay *empty*, so the plan's central claim that `prodLevel: 'error'` is behaviorally identical to the old numeric `0` ("errors only", not "nothing") had no positive coverage. It now pins: a module at `prodLevel: 'error'` drops `log`/`info`/`warn`/`debug` from both `prodLog` and history while `Loxer.error()` still reaches `prodError`; a module at `prodLevel: 'info'` *does* emit to `prodLog` (production is threshold-driven, not hardwired silent); default-init silence in prod; and `getModuleLevel` returning the threshold of the running mode. New `test/modules.test.ts` (3 tests) covers the two `Modules` fallback branches the ported suites missed — an unregistered module id rewriting `lox.moduleId` to `'INVALID'` (`Modules.ts:63-66`, previously uncovered) and the `?? 'info'` malformed-module fallback (`Modules.ts:74`) that keeps an untyped JS consumer's level-less module literal visible, asserted both as a unit and end-to-end through the public API. Two cases appended to `test/initialization.test.ts` prove the level gate runs at **replay** time against the table `init()` supplies, not at enqueue time — the plan's last Verification bullet, which the existing `queueing logs` test registered no modules for and so never exercised. 193 → 202 tests, 14 → 16 files, no pre-existing test changed or weakened; `pnpm test` and `pnpm typecheck:test` both exit 0. `src/core/Modules.ts` statements 88.63% → 93.18% (remaining uncovered there is only the `@deprecated` `getText`/`getColor` tail). I rewrote two comments the agent left in `production.test.ts` that used vocabulary the 23:18 entry had purged ("the quiet end of the scale", "channel") to the standing terms — *logs up to* for a threshold, *stream* for `prodLog`/`prodError`. |
| 2026-07-29 00:58 | [Testing] Product bug surfaced — pre-init `.m(id)` mislabels queued logs as `INVALID` | `m()` calls `ensureModule` eagerly at chain time ([src/Loxer.ts:152](../../../src/Loxer.ts#L152)) against the table that exists *then*; before `init()` that is the bare `new Modules()` ([src/Loxer.ts:55](../../../src/Loxer.ts#L55)) holding only `NONE`/`DEFAULT`/`INVALID`. So `Loxer.m('TEST').info(...)` before init replays with `moduleId: 'INVALID'` and is gated by `INVALID`'s threshold, colour, `fullName` and box layout even though `TEST` is registered in the `init()` call. Verified **pre-existing**: `ensureModule` is untouched by this diff and present verbatim at `HEAD`. Not fixed — this is a Testing pass and the fix (dropping the eager `ensureModule` so `Modules.getModule()`, which already falls back to `INVALID`, becomes the single decider) is a local but non-trivial change to the resolution path every log flows through. Surfaced to the user as a fix proposal instead. Consequence for coverage: the queue tests above had to drive the threshold through `defaultLevels` on `DEFAULT`/`NONE` rather than a user module id; the module-id variant is written up ready to add once the bug is fixed. Persisted with repro to [test-bugs.md](./test-bugs.md). |
| 2026-07-29 01:45 | [Testing] Debt register opened at `documentation/debt.md` | On the user's request, promoted the pre-init `.m(id)` bug out of this plan folder into a standing register so it outlives the change that found it. `documentation/debt.md` holds an index table plus one section per item (`D-1` is the `.m(id)` defect, written self-contained: where, symptom + repro, cause, impact, proposed fix with blast radius, and when it was found), a `## How to add an entry` contract, and a `## Resolved` section entries move to rather than being deleted. `test-bugs.md` stays as the record of the pass that found it; `debt.md` links back to it. Added a pointer in `documentation/AGENTS.md` and `rules/documentation.md` so later passes append instead of re-creating it, both noting it is a maintainer document — the "describe the current design, no diff-narration" rules govern the guides and deliberately do not apply to a register whose job is describing what is broken. Verified all four relative links resolve. |
| 2026-07-29 17:44 | [Reviewing] Pass 1 — verdict **WARN** ([review.md](./review.md)) | Five lenses dispatched in parallel: code, security, perf, test, acceptance. Skipped a11y (no user-facing UI — the only front-end file touched changes log calls, not markup) and the dep-audit half of security (no dependency or lockfile change; `package.json` gained only the `typecheck:types` script). **2 HIGH · 5 MEDIUM · 2 LOW**, no CRITICAL. The two HIGHs: (1) every Markdown image in `README.md` has been stripped to bare alt text — logo, five badges, both screenshots, plus three deleted comment lines and the trailing newline — unintended, outside the plan's stated README scope, and it will bake into `docs/index.html` on the next `pnpm docs` since TypeDoc renders the README as its front page (caught independently by code and acceptance; verified 7 `![` at `HEAD` vs 0 in the working tree). (2) `Modules.ts:74`'s threshold gate fails **open** on a defined-but-invalid level: `?? 'info'` guards only nullish, so `prodLevel: 0` from an untyped-JS Loxer 2 config reaches `isHidden`, where `LEVEL_ORDER[threshold]` is `undefined` and every comparison is `false` — the module then emits everything including `'debug'` in production, where the same config previously meant errors-only (verified by executing the formula). Recorded 14 checks as verified-sound so a later pass need not re-derive them (the gate's strict `>` and the `0 → 'error'` equivalence, `requestedLevel`/clamp asymmetry, the error bypass, replay-time gating, one-shot state, ported-suite fidelity across all 9 suites, the type-level negatives, the vocabulary purge and appendix). Five coverage gaps reported untouched for Documentation, loudest being **no spec** for a breaking major-version cut. |
| 2026-07-29 18:07 | [Reviewing] Pass 2 — verdict **WARN** ([review-2.md](./review-2.md)) | Reviewed the pass-1 fixes. Lenses: code, security, perf, acceptance (test skipped — no test file changed in a fix pass; a11y skipped). **1 HIGH · 5 MEDIUM.** The HIGH was **in my own pass-1 fix**: `resolveThreshold` validated with `threshold in LEVEL_ORDER`, and `in` walks the prototype chain, so `'constructor'`, `'toString'`, `'valueOf'`, `'hasOwnProperty'`, `'__proto__'` all passed as levels and reopened the identical production fail-open — `LEVEL_ORDER['constructor']` is the `Object` constructor, so the gate compared against a non-number and hid nothing. Found independently by code and security, both reproducing it against the built `dist/`; I verified it myself before accepting. The pass-1 vectors (`0`, `'off'`) *were* closed, so the fix was partial, not wrong. Confirmed resolved in this pass: the README restoration (faithful, all 7 images back, intended edits intact), `getLevel()`'s narrowed contract, the trace crash path, the Babel branch removal. **Rebutted one MEDIUM**: security wanted `resolveBoxLevel`'s fallback moved from `'info'` to `'debug'` so a typo fails toward hidden — but that same call site handles an *omitted* level, where `'info'` is the documented default (the pre-fix code was `o?.level ?? 'info'`), so `'debug'` would have regressed every trace that names no level. Put the rebuttal to the reviewer in pass 3; they accepted it and added that `resolveBoxLevel` never indexes a lookup table and has no gating effect at all. |
| 2026-07-29 18:07 | [Reviewing] Pass 3 — verdict **PASS** ([review-3.md](./review-3.md)) | Reviewed the pass-2 fixes with code, security and acceptance (perf skipped — the only hot-path delta was `Object.hasOwn` for `in`, and perf's standing "hoist the resolver out of the per-log path" MEDIUM already covers it either way; test skipped — no test file changed). **Both HIGHs confirmed RESOLVED.** Security and code each re-ran the attack against the built `dist/` across `'constructor'`/`'toString'`/`'valueOf'`/`'hasOwnProperty'`/`'__proto__'`/`'0'`/`0`/`'off'`/`null`/`undefined` and could not reopen it; symbol keys and `String` wrappers are gated out by the `typeof` check before `hasOwn`. Notably `src/core/color/parseColor.ts:93-95` already used `Object.hasOwn` with a comment naming this exact trap — the lesson was in the repo but not enforceable, which is now a recorded coverage gap. One new **MEDIUM**, fixed: `src/types.ts:28`'s JSDoc heading read "Get a module's level", contradicting its own next line ("logs up to") — the last surviving wording-rule violation, missed by two earlier sweeps because both swept `documentation/**` but not `src/**` JSDoc headings. Also cleaned a stray untracked `README_HEAD.md` that a reviewer's scratch dump had leaked into the repo root (inspected first — untracked regular file, not a symlink, content recoverable from git — then removed with a plain non-recursive delete). Wrote `review-2.md` and `review-3.md` after pass 3's acceptance lens caught that I had consolidated pass 2 only in chat and never persisted it, breaking the append-only review trail. |
| 2026-07-29 19:41 | [Finalization] Changelog | Created `CHANGELOG.md` at the repo root — the project had none. Scheme confirmed with the user as **Semantic Versioning 2.0.0** and written into the header's scheme sentence (detected from `package.json` + the `v1.0.0`/`v2.0.0`/`-beta` tag history). Scope also confirmed with the user: npm's latest published `loxer` is **2.0.0** and no `v3.0.0` tag exists, so `[Unreleased]` accumulates **everything since v2.0.0**, not just this plan — 7 plan folders plus two loose commits, sourced from each folder's `plan.md`/`worklog.md`/`review*.md` and cross-checked against `git diff HEAD` and `git log v2.0.0..HEAD`. **20 entries: Added 7 · Changed 5 · Removed 1 · Fixed 7.** From this plan: the named-level scale as a breaking `Changed` (with the `0 → 'error'` / `1 → 'info'` translation and the JS-config `'info'` fallback), `.l()`/`.level()` + `LevelType`/`LogLevelType` removal, the `warn`/`info`/`debug` methods on `Loxer` and `OfLoxes`, `getModuleLevel` → `undefined`, and two `Fixed` entries (the `defaultLevels` shared-const leak and the unrecognized-threshold fail-open). Also closes the `getModuleLevel`-narrowing changelog signal that `2026-07-26-typedmoduleids/worklog.md` left open. Deliberately **not** entries: pre-release defects in `babel-plugin-loxer-trace` / `vite-plugin-loxer-trace` (both 0.1.0, never published, so their internal fixes never reached a user), the Jest → Vitest migration and toolchain/CI/lint config work (development-only), and the vendored-parser hardening introduced in the same cycle. **Implied bump: MAJOR — 2.0.0 → 3.0.0** (four `**Breaking:**` entries plus a public-API `Removed`); `package.json` already reads `3.0.0` from the modernization commit, so no manifest needs touching and none was. Flagged for the user: the two companion packages are separately versioned at an unpublished `0.1.0` and warrant their own `CHANGELOG.md` when first released. |
| 2026-07-29 19:47 | [Finalization] Commit | [2026-07-28-standardloglevels] Add named log levels and type-safe module ids |
