# Worklog — 2026-08-01-realmsingletonpendingqueue

> **How to log:** read this file first, then add your row at the **bottom** of the table, below the last existing row (newest last — never insert after the header). The **Timestamp** is `YYYY-MM-DD HH:MM` from a real clock (`Get-Date -Format "yyyy-MM-dd HH:mm"` / `date "+%Y-%m-%d %H:%M"`) — never guess the time. Prefix each **Action** with the workflow phase in brackets — e.g. `[Planning]`, `[Implementation]`, `[Testing]`, `[Reviewing]`, `[Documentation]`, `[Finalization]` — so every entry is traceable to the phase that wrote it.

| Timestamp | Action | Detail |
|-----------|--------|--------|
| 2026-08-01 16:15 | [Planning] Plan written | Realm-scoped singleton (`src/core/Realm.ts`, major-keyed `Symbol.for`), in-place `resetLoxer()`, running-id counter moved from `Lox` static onto the instance, bounded + self-reporting pre-init queue (5s one-shot warning, cap 1000, drop-newest), `config()` hook on `vite-plugin-loxer-trace` and its missing README. Grounded by the inline survey of `src/Loxer.ts`, `src/core/Loxes.ts`, `src/loxes/Lox.ts`, `src/trace.ts` and the plugin; no spec, no new dependency. Two empirical questions left open for the reporting app. |
| 2026-08-01 16:23 | [Implementation] Changes 1-4 landed | `src/core/Realm.ts` (new, import-free, `Symbol.for('loxer.realm.3')`, non-extensible-record + defineProperty-throw fallbacks to module-local slots); `export const Loxer = realmSlot(...)` with `resetLoxer()` delegating to a new in-place `LoxerInstance.reset()`; `Lox._runningId`/`nextId`/`resetStaticRunningId` deleted, `LoxProps.id` now required, counter moved to `LoxerInstance.nextId()` feeding all four `new Lox(...)` sites; `Loxes` gained the 1000-entry cap with drop-newest, a fire-once 5s `unref()`'d report timer, the elapsed-time backstop on `enqueue`, drop-count reporting in `dequeue()`, and `dispose()` for reset. `pnpm build`, `pnpm lint`, `pnpm test` (246 tests, 18 files) all exit 0 with no existing test touched. |
| 2026-08-01 16:24 | [Implementation] Duplicate-copy and queue behavior verified out-of-band | The new suites belong to the Testing phase, so verification ran as throwaway scripts against two independent copies of `dist/` in the session scratchpad (a real duplicate-module-tree, not a mocked registry). Confirmed: both copies resolve to one instance, a log through copy B reaches copy A's callbacks and history, ids from the two copies stay distinct and each `close()` finds its own box, and a held `const L = Loxer` observes `resetLoxer()`. Queue: overflow warns once naming the cap and both causes, the head survives while the newest is dropped, replay keeps exactly 1000 and reports 202 dropped, a healthy pre-init gap warns nothing, `resetLoxer()` silences a pending queue, and the real 5s timer reports once and never again. |
| 2026-08-01 16:25 | [Implementation] Change 5 landed, plan risk 1 answered | `vite-plugin-loxer-trace` gained a `config()` hook contributing only the `optimizeDeps.include` / `resolve.dedupe` entries the user has not already set (Vite concatenates arrays on merge, so contributing the full list would duplicate the user's own), plus a `dedupe` option to opt out. **Plan risk 1 is answered empirically**: in `examples/vite-trace-demo`, `_metadata.json` held `optimized: {}` before the change — Vite pre-bundled neither entry, which is exactly the late-discovery condition — and after it holds both `loxer` and `loxer/trace` sharing **one** `Loxer-BywM_StU.js` chunk from a single optimize run. An already-optimized entry cannot be discovered mid-session, so the re-optimization trigger is closed. |
| 2026-08-01 16:29 | [Implementation] Docs, packaging and verification closed out | Added `packages/vite-plugin-loxer-trace/README.md` (the package shipped none) and `README.md` to its `files`, plus the realm-scoping and pre-init-queue paragraphs in `documentation/index.md`'s init section. `pnpm build` / `pnpm lint` / `pnpm test` (246 tests) and typedoc all exit 0. Two deviations from the plan's expectations, both benign: (a) the predicted `.d.ts`/TypeDoc loss of the inherited static **did** land in `docs/classes/Logs.{Output,Error}Lox.html` as expected, but the regeneration also picked up **pre-existing** drift in `index.ANSIFormat.html` (`bgWarn`→`bgError`) and `Loxer.{OfLoxes,OpenedLox}.html` (level-visibility wording) — `src/core/ANSIFormat.ts` last changed in HEAD (4edbfdd) while its page was last generated in a1dda91, so that drift belongs to the previous commit, not this change; (b) `pnpm docs` prints nothing and returns 0 without regenerating anything, because `docs` is a **built-in npm/pnpm command** ("open documentation for a package in a web browser", alias `home`) that shadows the `docs` script — `pnpm run docs` is what actually runs typedoc. (An earlier version of this row blamed output redirection in Git Bash; that was wrong — an un-redirected `pnpm docs` no-ops identically, and `pnpm docs --help` prints npm's `docs` help, which is what confirmed the real cause.) |
| 2026-08-01 16:29 | [Implementation] Deferred to later phases | Left undone deliberately, all plan-scoped: the new suites (`test/realm-singleton.test.ts`, the queue coverage in `test/initialization.test.ts`, the `config()` hook unit test) belong to the Testing phase, and the `CHANGELOG.md` `[Unreleased]` entry to Finalization. Plan risk 2 (how long after module evaluation a real app's `init()` runs, calibrating the 5s threshold) is still open and only the reporting app can answer it; the live confirmation in that app is likewise outstanding. |
| 2026-08-01 16:38 | [Testing] Vite `config()` hook covered | Six cases appended to `test/vite-plugin-loxer-trace.test.ts` (11 total, all green via `pnpm exec vitest run test/vite-plugin-loxer-trace.test.ts`), with a `runConfig` helper mirroring `runTransform` that resolves either Vite hook shape (bare function or `{ handler }`). Covered: both entries plus `resolve.dedupe: ['loxer']` for an empty *and* an empty-sectioned user config; contribution narrowed to only the missing entry for a user who already lists `loxer` (and `react`), asserted as a *partial* config Vite concatenates rather than a merged whole; the same for `resolve.dedupe`; the `optimizeDeps` / `resolve` key omitted entirely when nothing is missing from it; `null` rather than `{}` when the user lists everything; `dedupe: false` contributing nothing while the transform keeps tracing; and the user's own arrays asserted unmutated. No product bug found — the hook matches the plan's change 5 exactly, so no `test-bugs.md` was created. One test-level fix outside the plugin suite: `test/modules.test.ts:35` passed `id: undefined`, which stopped typechecking when change 3 made `LoxProps.id` required (the plan's change-3 note predicted all three direct-`Lox` test files already passed explicit ids — `modules.test.ts` did not); it now passes `id: 0`, and `pnpm typecheck:test` exits 0. |
| 2026-08-01 16:46 | [Documentation] Session lessons landed in the steering docs | Five doc-writer dispatches, batched by target. **`pnpm docs` is a built-in npm/pnpm command** ("open a package's documentation in a browser", alias `home`) that shadows the `docs` script, so it exits 0 and regenerates nothing — `pnpm run docs` is the real command. Corrected in `AGENTS.md` (Commands), `rules/documentation.md` (all mentions plus its unfalsifiable "done only when `pnpm docs` exits 0" gate, now resting on typedoc's own "html generated at ./docs" and a changed `docs/` tree) and `documentation/AGENTS.md`. Recorded the realm invariants: `AGENTS.md` Behavior gained the `globalThis`-slot instance, the never-rebind-the-export rule with its reason (a rebind is invisible to a held `const L = Loxer` and to a second copy), the instance-owned id counter, and the bounded self-reporting queue; `src/core/AGENTS.md` gained the `Realm.ts` invariants (import-free, major-keyed anchor, never-throw fallback) and the drop-newest-never-the-head rule with its `findOpenLox` reason. `rules/testing.md` gained the gap this change walked into: **`pnpm typecheck:test` is now required for any `src/` change that alters a type tests construct directly**, because `build`/`lint`/`test` all stay green while `test/` silently stops typechecking — and its stale `.eslintrc`/`ignorePatterns` citation is corrected to `eslint.config.mjs`'s `ignores` array. |
| 2026-08-01 16:45 | [Testing] Realm-singleton and pre-init-queue suites covered, phase closed | New `test/realm-singleton.test.ts` (7 cases) reproduces the reported duplicate-copy condition with `vi.resetModules()` — a fresh module registry while `globalThis` survives — and asserts: two copies resolve to one `Loxer` (guarded by `expect(b.resetLoxer).not.toBe(a.resetLoxer)` first, so the shared-identity assertion cannot pass vacuously on a registry that failed to reset), a log through copy B reaching copy A's callbacks and history, a module registered through one copy known to the other, four interleaved opens across both copies getting distinct ids with each `close()` finding its own box (the moved-id-counter regression guard — a collision surfaces as the missing-box error path, so `devErrors` staying empty is load-bearing), the shared counter counting up across copies, and `resetLoxer()` observed both by a held `const L = Loxer` and through the other copy. Slot cleared via `clearRealmSlot('instance')` in `afterEach`; cross-file leakage **verified not assumed** — probe files showed a distinct `process.pid` per test file (forks pool, `isolate: true`), so the realm slot cannot leak between files, but within-file clearing is genuinely required. Five queue cases added to `test/initialization.test.ts` under `vi.useFakeTimers()`: the report fires exactly once past 5s (naming count, elapsed ms, both candidate causes and the first queued message) and never again on further advance or enqueue while the queue still replays complete; `init()` inside the threshold warns nothing and leaves `vi.getTimerCount() === 0` and cannot fire late; `resetLoxer()` with a pending queue reports nothing on a later advance; overflow reports immediately, replays exactly 1000, reports the drop count separately at replay, and drop-newest is asserted concretely (head `queued 0` survives, newest absent); and one functional head-survival case where a queued `open` at the head still pairs with its retained `close` through an overflow, since `findOpenLox` searches the pending queue. The three existing queue-replay/ordering tests are unmodified. The suites' guards were negative-controlled against six scripted `src/` mutations (id counter back to module scope, `new LoxerInstance()` instead of `realmSlot`, drop-oldest, `_hasWarned` removed, `reset()` without `dispose()`, `dequeue()` without `disarmReport()`) — all six fail; `src/` restored byte-identically and `git diff -- src/` confirms only the implementation's own changes. Full `pnpm test` 264 tests / 19 files and `pnpm typecheck:test` both exit 0; `src/core/Loxes.ts` at 100% statements/lines, `src/core/Realm.ts` 95.8%. **No product bug found, so no `test-bugs.md` exists.** Two notes, neither a defect: (a) `PENDING_QUEUE_CAP` / `PENDING_QUEUE_TIMEOUT_MS` are deliberately unexported, so the tests mirror them as local constants with a pointer comment — changing either constant fails the tests on a value mismatch rather than adapting, which is the accepted cost of having no configuration knob; (b) `_hasWarned` lives on `Loxes` and `reset()` installs a fresh one, so the report is once per pre-init epoch rather than literally once per instance object — correct behaviour (a new epoch deserves a new diagnosis) but narrower than the "once, ever, per instance" wording in `src/core/Loxes.ts`'s JSDoc, left for the Documentation phase. Untested by design: the `unref()` claim, which needs a spawned Node process expected to exit rather than an assertion on a private timer. |
| 2026-08-01 16:51 | [Documentation] Session lessons swept into `rules/testing.md` | One lesson landed via doc-writer, additively, in the repo's existing bare `rules/*.md` convention (plain Markdown, no frontmatter, already indexed in the `AGENTS.md` steering table): a suite that loads a second copy of Loxer (`vi.resetModules()` then re-`import`) must also call `clearRealmSlot('instance')` in `afterEach`, because the realm slot on `globalThis` deliberately outlives a module-registry reset and is therefore the one piece of global state `resetLoxer()` does not clear; such a suite gets its **own** test file, since `vi.resetModules()` breaks module identity for anything imported after it; and cross-file leakage of realm state must be **verified rather than assumed in either direction** — re-probed this session and each test file reported a distinct `process.pid` (including under `--maxWorkers=1`) because `vitest.config.ts` sets neither `pool` nor `isolate`, so adding `pool: 'threads'` or `isolate: false` would invalidate the conclusion without touching a single test. Two probe files were created for that verification and removed. Deliberately not re-recorded because they were already on disk from earlier in the session: the realm-scoping / in-place-reset / instance-id-counter / bounded-queue invariants (root `AGENTS.md`, `src/core/AGENTS.md`), the shadowed-`pnpm docs` finding (`AGENTS.md`, `documentation/AGENTS.md`, `rules/documentation.md`), and the `pnpm typecheck:test` gate for type-affecting `src/` changes (`rules/testing.md`, staged earlier). Dropped as too narrow or as our own practice rather than user-endorsed: the one-line `test/modules.test.ts` `id: undefined` fix, the mirrored unexported queue constants, and the negative-control-your-guards practice. **Index note, not normalized:** the doc-writer left the worktree fully staged (`git add`) apart from its own `rules/testing.md` edit; nothing was committed (HEAD still `4edbfdd`), no stashes, and the index was left exactly as found. |
| 2026-08-01 17:05 | [Reviewing] Review loop completed | Three passes recorded in `review.md`, `review-2.md`, and `review-3.md`; pass 1 found pre-init diagnostics exposing queued message content, fixed by emitting metadata only; pass 2 corrected the stale overflow expectation and staged both fixes; pass 3 is PASS with only non-gating documentation/coverage gaps. `pnpm lint`, `pnpm build`, and `pnpm typecheck:test` passed after the source amendment. Full-suite baseline and final regression gate both passed: 19 files / 264 tests. |
| 2026-08-01 23:55 | [Finalization] Changelog | Added 3 Fixed entries under [Unreleased] in `CHANGELOG.md`; accumulated Unreleased entries imply a MAJOR release under Semantic Versioning (next version 4.0.0). |
| 2026-08-01 23:58 | [Finalization] Commit | [2026-08-01-realmsingletonpendingqueue] Share Loxer state across module copies |
