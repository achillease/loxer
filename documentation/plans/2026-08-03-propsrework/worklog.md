# Worklog — 2026-08-03-propsrework

> **How to log:** read this file first, then add your row at the **bottom** of the table, below the last existing row (newest last — never insert after the header). The **Timestamp** is `YYYY-MM-DD HH:MM` from a real clock (`Get-Date -Format "yyyy-MM-dd HH:mm"` / `date "+%Y-%m-%d %H:%M"`) — never guess the time. Prefix each **Action** with the workflow phase in brackets — e.g. `[Planning]`, `[Implementation]`, `[Testing]`, `[Reviewing]`, `[Documentation]`, `[Finalization]` — so every entry is traceable to the phase that wrote it.

| Timestamp | Action | Detail |
|-----------|--------|--------|
| 2026-08-04 00:07 | [Specification] Spec written | `documentation/specs/props-rework.md`. Grounded by an architect domain survey and a web-researcher prior-art pass (console/`util.format`, pino, winston, consola/tslog). Four framing calls taken with the user: clean break inside unreleased 3.0 (npm `latest` is 2.0.0 — the whole `[Unreleased]` section has never shipped, so "additive" protected nobody); the noun "item" dropped entirely for **props**; per-log opt-in printing via a chained modifier where an empty config still means render; freely-typed first argument stringified at capture with `lox.message` staying `string`. Two live defects pulled into scope because they are most of why the feature is unused: `Item` is exported from no public entry point while two guides teach `Item.of(lox).prettify(...)`, and `@trace`'s `argsAsItem`/`resultAsItem` pass two arguments so the options slot has never been reachable from a trace. Recorded as a constraint, measured not assumed: `(msg, ...props, options?)` is not expressible in TypeScript (`TS1266`), and a *required* trailing options tuple typechecks but forces options on every call — so moving options onto the chain is the only checkable design. |
| 2026-08-04 00:07 | [Planning] Plan written | `documentation/plans/2026-08-03-propsrework/plan.md`. Grounded by an architect technical survey; web-researcher skipped (zero new dependencies — `util.format`/`inspect` are Node-only and the browser is a live target). Five spec open questions settled with the user: trace rendering configured **separately** for args and result; `depth` gains a real sentinel (absent = unlimited, `0` a usable limit) fixing a JSDoc that claims `infinity` while the code compares `0`; hard single-line message mode, no truncation; several props render as one block, array-like without the brackets; `namedError` drops `existingError`. The survey found three defects in the spec, all fixed before planning: `Loxer.log()` vs `Loxer.log(undefined)` are indistinguishable through a default parameter (criterion amended — an empty message wins over the distinction); the `namedError` criterion was **unsatisfiable** as written, since a rest parameter cannot rescue an optional `unknown` in front of it (resolved by dropping `existingError`, whose wrap path `NamedError`'s own JSDoc already teaches); and two DoD items pointed at files that carry no reference — the glyph assertions live in `test/item.test.ts`, not `test/boxed.test.ts`, and the guide link is `documentation/index.md:970` plus `documentation/AGENTS.md:12`, not `README.md`/`src/Loxer.ts`. Plan sequences the work in 7 steps to keep the red-build window to one atomic step, and flags six expressions where a rename and a behavior change share a token. Note: the harness `seed-plan.js` hardcodes `<project>/docs/plans`, which typedoc's `cleanOutputDir` wipes — folder seeded in the scratchpad and placed under `documentation/plans/` per the project rules, worklog header byte-identical. |
| 2026-08-04 01:05 | [Implementation] Code, tests and the built trees green | Steps 1–6 of the plan landed. `src/core/Item.ts` → `src/core/PropsPrinter.ts` (`Item`→`PropsPrinter`, `prettify`→`print`, lox-free `ofValues`/`singleLine` entries, N-prop list rendering, hard single-line mode, `depth` sentinel, the swapped `box` JSDoc fixed); `LoxProps` renamed to **`LoxInit`** — the plan kept the old name, but `LoxInit.props` vs `lox.props` would have been one word for two things, which `rules/documentation.md` bans, and nothing outside `Lox.ts` referenced it; `props: unknown[]` + `printProps` on the lox; `pp`/`printProps` modifier; `namedError` promoted onto `LogMethods` and stripped of `existingError`; both trace runtimes spread args to one prop each and chain the modifier only where `printArgs`/`printResult` asked, behind one `needsPrintProps` gate. Two deviations worth recording. (1) **`prettyResult` trace messages are now escaped.** `stringifyMessage` sanitizes every message, not only a stringified object — which the plan explicitly wanted ("closes a pre-existing hole: `Loxer.log('a\nb')` already corrupts the box column") — so `closeMessage: 'prettyResult'`, whose whole output is indented JSON, now reads its newlines as their escaped form. Kept, because a multi-line message shatters the box column, the `OPEN_LOGS` summary and highlight coloring alike; recorded as debt rather than silently narrowing the sanitizer. (2) Trace rendering is configured through **new** options `printArgs` / `printResult` (`boolean | PropsPrinterOptions`) rather than by widening `argsAsProps`, so capture and rendering stay independently settable — the spec asked for both, separately, per side. Gates: `pnpm lint`, `pnpm test` (350 pass, `test/item.test.ts` → `test/props.test.ts`, 50 cases), `pnpm build`, `pnpm typecheck:test`, `pnpm typecheck:types`, `pnpm demo:build` all exit 0. Beyond the gates: `playground/items.js` → `playground/props.js` and `playground/OrderService.js` both run green against `dist/`, and a scratch harness ran the **Babel plugin's `dist/` transform output** against `dist/trace.js` + `dist/index.js`, pinning the arg spread, the single result prop, both `print*` options reaching their own log, and a prop arriving by reference. |
| 2026-08-04 01:15 | [Implementation] Docs, guide and CHANGELOG | Step 7. `documentation/item.md` → `documentation/props.md`, rewritten as if props had always been the design, with the four Loxer-output screenshots replaced by **output captured from `dist/` itself** (a scratch script re-ran the guide's scenarios with `disableColors: true`) — a PNG cannot be regenerated without a terminal screenshot, and text blocks are reproducible and reviewable where an image is neither. `documentation/index.md`: every signature updated to `(message?, ...props)`, a new `### Rendering props` subsection under section 2 (no renumbering), both `OutputStreams` callback examples switched to `PropsPrinter.of(lox).print(...)` behind `if (lox.printProps)`, the `namedError` shortcut rewritten around the explicit wrap path, and an `### Items` table added to the existing Loxer 2 appendix — the only place, with the CHANGELOG, where the old noun is allowed to appear. `CHANGELOG.md` `[Unreleased]`: five Breaking entries (parameters replaced, concept renamed, arg capture spread per argument, `existingError` dropped, `depth` sentinel) plus the escaping note, and the stale `ItemType`/`ItemOptions` promise in the Added block corrected to `PropsPrinterOptions`. `documentation/debt.md` gains **D-2** for the `prettyResult` escaping, with the argument for retiring the style rather than narrowing the sanitizer. `pnpm run docs` regenerated (typedoc printed "html generated at ./docs"; `docs/classes/Formatting.Item.html` deleted, `Formatting.PropsPrinter.html` added) and **every** `pcprinz.github.io` link in both guides was then checked against the fresh tree — which also surfaced two pre-existing dead links, `ANSIFormat` and `BoxFactory`, that have lived under `classes/index.*` rather than `classes/Formatting.*`; fixed in passing. Consumer app: `examples/vite-trace-demo/node_modules/.vite/deps` held a frozen Aug-1 `dist/` and was removed (listed first — its sibling `node_modules/loxer` is a symlink to the repo root, so only the plain `deps` directory was touched), after which `pnpm demo` serves `/@fs/C:/dev/loxer/dist/index.js` directly: the transform emits `argsAsProps`/`resultAsProps`/`printArgs`/`printResult` and the served `Loxer.js` carries `pp`/`printProps`. The browser click-through is **not** done — `playwright` is not installed in this workspace. Left for the Documentation phase: the steering docs still name `test/item.test.ts`, `documentation/item.md`, `playground/items.js`, `Item`/`prettify` and the `if (outputLox.item)` gate (`AGENTS.md:5,38,50`, `src/core/AGENTS.md:5,55,75`, `src/decorators/AGENTS.md:14,24`, `rules/documentation.md:84`, `rules/testing.md:43-51,101`). |
| 2026-08-04 12:14 | [Reviewing] Pass 1 — **BLOCK** | `review.md`. Six lenses run in parallel (code, simplicity, security, perf, acceptance, test); a11y skipped as a library change with no user-facing UI, and security's dep-audit half skipped because no manifest or lockfile changed. 14 findings after de-dupe: **1 CRITICAL** — `PropsPrinter.prettifyValue` guards `value.constructor?.name` on line 279 and then reads `value.constructor.name` unguarded on 281 and 284, so `Loxer.log(Object.create(null))` throws `TypeError` out of the logger; the bug is copied from `Item.ts` but this change is what makes it reachable, since the new `stringifyMessage` runs `PropsPrinter.singleLine` on every non-primitive message with no `.pp()` involved. **4 HIGH** — no `try/catch` around the printer's property access, which turns a throwing getter into an unhandled rejection on the new async `printResult` path; props rendered *without* the control-character/ANSI sanitization this same change added for messages, in a second output path `OutputStreams` reaches directly; `stringifyMessage` running ahead of the level gate, the exact anti-pattern `AGENTS.md`'s last Behavior bullet names, and new cost since `message` used to already be a `string`; and the `printArgs`/`printResult` gate untested for the plain-function runtime — zero `printArgs|printResult` matches across all three `plain-function-trace*` files, against a project rule that both sides of such a gate need a test. 6 MEDIUM, 2 LOW. Five coverage gaps for the Documentation phase, headed by the absence of any `SECURITY.md` — the sanitization discipline is a de facto rule nothing in `rules/` indexes, which is plausibly why the props path diverged from the message path unnoticed. A "checked and explicitly not flagged" section records 17 things the lenses cleared (the renames left nothing orphaned, `ofValues`/`singleLine` have real callers, the `depth` sentinel is covered, `test/props.test.ts` correctly inits without `devLog`/`devError`, D-2 and the deferred steering docs not re-reported) so a later pass does not re-litigate them. Not architectural — a list of local fixes, so the next step is fix-and-re-invoke, not a re-plan. |
| 2026-08-04 14:18 | [Reviewing] Pass 2 — **WARN** | `review-2.md`. Six lenses reran on the staged feature plus pass-1 fixes; a11y and dependency audit remained out of scope. After de-dupe: **3 HIGH**, 7 MEDIUM, 1 LOW. Gating findings are incomplete terminal-control sanitization, missing plain-function `printArgs`/`printResult` coverage, and weak hostile-prop/async-render coverage. The continuation baseline and all non-visual gates were green (19 files / 364 tests). |
| 2026-08-04 14:32 | [Reviewing] Pass 3 — **WARN** | `review-3.md`. Six lenses reran after pass-2 product fixes. After de-dupe: **3 HIGH**, 3 MEDIUM, 1 LOW. Earlier runtime findings cleared; the remaining runtime gate is an overridable, unsanitized `Date.toISOString()`, while plain-function trace, hostile-prop/async-render, and rendered-sanitizer coverage remain open. |
| 2026-08-04 14:43 | [Reviewing] Pass 4 — **WARN** | `review-4.md`. Terminal review after the third/final fix pass. Code, simplicity, and security found no gating runtime defect; **3 HIGH** coverage findings remain (plain-function marker rendering, hostile prop/async rendering, and rendered sanitizer boundaries), plus 4 MEDIUM and 1 LOW. The fix-pass cap is reached. |
| 2026-08-04 14:44 | [Reviewing] Review loop closed — **WARN** | Four review passes total (`review.md` through `review-4.md`) and three fix passes. Exit: fix-pass cap reached with 3 HIGH test-coverage findings still open; product-code/security lenses are clean. Mandatory final `pnpm test`: 19 files / 364 tests passed, matching the continuation baseline. No post-review product edits. |
| 2026-08-04 15:14 | [Testing] Added regression coverage for review findings | `test/props.test.ts` now covers hostile console-rendered props (null prototypes, throwing getters/proxies, invalid dates), async trace-result rendering, intrinsic Date/function formatting, and control-character escaping in strings, keys, and symbols. `test/plain-function-trace-core.test.ts` and `test/plain-function-trace-enclosing.test.ts` cover independent `printArgs` / `printResult` propagation for direct, shared-list, enclosing, and async markers. Test-only expectation corrections were made after the first run; no product bug was found. Gates: `pnpm test` 19 files / 376 tests passed; `pnpm typecheck:test` and `pnpm typecheck:types` passed. |
| 2026-08-04 15:21 | [Reviewing] Review loop — **PASS** | `review-5.md`. Six lenses reran on the complete worktree; a11y and dependency audit were out of scope. No CRITICAL or HIGH findings. Seven non-gating findings remain: literal unbounded public layout depth and recursive deep-message rendering require a product decision to change the approved unlimited-depth contract; plain-only rendering is a LOW optimization; two guide examples and the steering docs are Documentation-phase work; and two additional test-coverage cases belong to Testing. Final suite run follows this review record. |
| 2026-08-04 17:35 | [Reviewing] Review loop — **PASS** | `review-6.md`. The final parallel code/performance, simplicity/security, and acceptance/test/doc/public-type passes all returned **NO FINDINGS** and no coverage gaps after resolving every prior finding. Final gates: `pnpm lint`, `pnpm build`, `pnpm typecheck:test`, `pnpm typecheck:types`, `pnpm run docs` (generated successfully; 19 existing non-fatal TypeDoc link warnings), and `pnpm test` (19 files / 396 tests) all passed. `git diff --check` found no whitespace errors. |
| 2026-08-04 18:02 | [Finalization] Changelog | Added 2 Fixed entries under [Unreleased] in `CHANGELOG.md`; Semantic Versioning implies MAJOR (3.0.0 from the last released 2.0.0) because the accumulated unreleased entries contain public breaking changes. |
| 2026-08-04 18:03 | [Finalization] Commit | [2026-08-03-propsrework] Replace items with props |
