# Worklog — 2026-08-14-markeronlytracing

> **How to log:** Add each new row at the bottom of this table. Use a real `YYYY-MM-DD HH:MM` time (`Get-Date -Format "yyyy-MM-dd HH:mm"` / `date "+%Y-%m-%d %H:%M"`). Start **Action** with the phase, for example `[Planning]` or `[Implementation]`.

| Timestamp | Action | Detail |
|-----------|--------|--------|
| 2026-08-14 11:58 | [Planning] Plan written | Marker-only tracing: delete `@trace` + `@initLoxer`, rename the freed types, port per-side highlight to the marker chain, retire decorator toolchain config. Grounded by an architect technical survey. |
| 2026-08-14 12:04 | [Planning] Scope narrowed | All documentation deferred on user request — guide, JSDoc, TypeDoc regeneration, changelog and steering docs. Only `typedoc.json`'s dead entry point is removed, because it breaks `pnpm run docs`. Deferred list recorded in the plan. |
| 2026-08-14 12:19 | [Implementation] Streams 1–6 landed | Deleted `src/decorators/`, both root-barrel re-exports, the decorator-only props block in `PropsPrinter.ts`, and `src/core/TraceNames.ts` (`qualifiedFunctionName` folded into `TraceMessage.ts` as a private helper; `classParentName` had no caller left). Renamed `TraceMarkerOptions` → `TraceOptions`, `TraceMarkerRuntimeOptions` → `TraceRuntimeOptions`, `ExtendedPropsPrinterOptions` → `TracePrintOptions`. Made `isHighlighted` side-aware and widened `h()`/`highlight()` to `boolean \| TraceHighlight`; `TraceHighlight` is now exported from `loxer/trace`, since a caller naming the argument type needs it. Dropped the memo from `parentNameResolver`, keeping the laziness. |
| 2026-08-14 12:19 | [Implementation] Toolchain retired | Removed `experimentalDecorators` from `tsconfig.json` and `test/types/tsconfig.json`, the `oxc.decorator.legacy` block from `vitest.config.ts`, and the `src/decorators/index.ts` entry point from `typedoc.json`. `pnpm build` covers both workspace packages that extend the root tsconfig — removal is inert, as the plan predicted. |
| 2026-08-14 12:20 | [Implementation] Tests | Deleted the three decorator suites and `test/trace-cases.ts` (~1292 lines). Surgery on `test/trace-message-cases.ts` (imports the renamed type from `../src/trace`), `test/class-parent-name-cases.ts`, `test/plain-function-trace-enclosing.test.ts` and `test/types/registry.test-d.ts` — the `@ts-expect-error` pins on the removed option keys were re-read row by row and kept, and two new pins assert `trace` / `initLoxer` are gone from the package root. Added `h('open')` / `h('close')` / `h('all')` marker rows covering the open log, the close log and the failure close. |
| 2026-08-14 12:21 | [Implementation] Deviation: memoization tests | Three rows in `test/trace-message.test.ts` asserted the memo that stream 4 deliberately removes (`toHaveBeenCalledTimes(1)` on a repeated parent read). The plan's test list did not name this file. Rewrote them to pin what survives — deferral until first use, and the same parent rendered on both sides and on repeated `parentFn` calls — rather than deleting the coverage. |
| 2026-08-14 12:24 | [Implementation] Stale `dist/` cleaned | `tsc` does not clean, so `dist/decorators/` and `dist/core/TraceNames.*` survived the rebuild as orphans. `package.json` publishes `files: ["dist"]` wholesale, so they would have shipped. Removed the listed files individually and rebuilt; `dist/index.js` now exports neither `trace` nor `initLoxer`. |
| 2026-08-14 12:26 | [Implementation] Verification | All five gates exit 0 (`lint`, `test` 559 passing, `build`, `typecheck:test`, `typecheck:types`). Repo sweep for the nine straggler terms is clean — every remaining hit is a deferred doc, plus `rules/coding-conventions.md:5`, which the plan's deferred list missed (see the next row). Built trees exercised: a module transformed by `packages/babel-plugin-loxer-trace/dist` and run against `dist/trace.js` + `dist/index.js` confirms boxes, the trailing-`Class` parent, and per-side highlighting including the failure close; `node playground/OrderService.js` exits 0; typedoc runs with 0 errors into a scratch `--out`, leaving `docs/` untouched. |
| 2026-08-14 12:27 | [Implementation] Demo check partly unobservable | The demo confirms traced calls still open and close boxes (sync, failed-async and overlapping scenarios all pair correctly) and that the Vite transform runs, with no console errors. It cannot show the plan's `h('open')` check: `examples/vite-trace-demo/src/main.ts` never reads `lox.highlighted`, so no entry renders highlighting either way — a gap in the demo, not in the change. Closed the gap instead by asserting what the built Vite plugin emits: `h('open'\|'close'\|'all')` pass through opaquely and a bare `h()` still emits `true`, confirming the plan's "no build-time change" claim end to end. A temporary one-line probe in the demo was reverted; `git status examples/` is clean. |
| 2026-08-14 12:28 | [Implementation] Deferred list needs one addition | `rules/coding-conventions.md:5` states `experimentalDecorators: true` as part of the stack and is not in the plan's deferred steering-doc list (which names `AGENTS.md`, `rules/testing.md` and `src/core/AGENTS.md`). It is now false and is read as instruction, so it belongs with the other steering-doc items the documentation follow-up picks up first. |
| 2026-08-14 12:56 | [Reviewing] WARN — pass 1, `review.md` | 5 agents dispatched (code, simplicity, perf, acceptance, test; security and a11y N/A). 9 findings: 2 HIGH, 3 MEDIUM, 4 LOW; 0 CRITICAL. Queue: 8 🟢 local, 1 🟡 contained, 0 🔴 redesign. Two HIGH: a dead `traceDecorator` row at `test/types/registry.test-d.ts:198-199` whose `@ts-expect-error` now swallows a "Cannot find name" error, so the pin cannot fail; and three documentation files (`CHANGELOG.md`, `documentation/environments.md`, `documentation/index.md`) staged in this tree that plan stream 7 defers in full. Simplicity and perf both returned no findings — the memo removal and the widened `h()` were checked and cleared. |
| 2026-08-14 13:01 | [Implementation] Review pass 1 fixes | `TEST-stale-ts-expect-error-tracedecorator`: fixed. `CODE-trace-options-literal-claim`: fixed. `CODE-message-examples-dont-compile`: fixed — verified by compiling all five `@example` forms against `dist/` through a temporary `test/types/*.test-d.ts` probe, and by confirming the pre-fix forms still error (`TS2344`, `TS2554`); probe deleted, `test/types/` clean. `CODE-trace-highlight-undocumented`: fixed. `CODE-removed-root-exports-inventory`: fixed — plan stream 7's migration-appendix bullet now carries the full inventory of names the deleted barrel took off the root. `ACC-deferred-item-not-durable`: fixed — plan stream 7's steering-doc bullet now names `rules/coding-conventions.md:5`, plus the `AGENTS.md` memoization rule and the three deleted paths `src/core/AGENTS.md` cites. `ACC-no-spec`: deferred — a spec cannot be written for work already built, and the user-visible contract it would state belongs to the deferred documentation follow-up; plan stream 7 now records that the follow-up warrants its own spec, written by `specify`. `ACC-undeclared-doc-edits-in-tree` and `ACC-demo-frozen-deps-unrecorded` were not in this pass — both need a user decision. All five gates exit 0 (`lint` 0 errors, `test` 559 passing, `build`, `typecheck:test`, `typecheck:types`); `dist/` carries no decorator orphans. |
| 2026-08-14 13:10 | [Implementation] Demo rerun on a cleared cache | `ACC-demo-frozen-deps-unrecorded`: fixed. Cleared `examples/vite-trace-demo/node_modules/.vite/deps` (two plain files, no links, `optimized: {}` — nothing had ever been pre-bundled) and reran `pnpm demo` on port 5174. The freshness question is settled more strongly than by the clearing itself: `vite.config.ts` sets `optimizeDeps.exclude: ['loxer', 'loxer/trace']`, so Vite resolves the package to `/@fs/C:/dev/loxer/dist/index.js` and `/@fs/C:/dev/loxer/dist/trace.js` and serves the working `dist/` directly with no cache layer. The served `trace.js` carries the side-aware `isHighlighted(highlight, side)` at all three sites including the failure close, and the served `index.js` mentions neither `initLoxer` nor `decorators`. `app-inspector` then clicked all four demo buttons: sync nests and closes (`calculateTotal(59.85) done`), the throwing scenario closes rather than hanging (`chargePayment failed` → `main.submitOrder failed`, original rejection preserved), and all seven boxes of the overlap scenario pair with their own close with no cross-linking (`#8` stays order 101, `#10` stays order 102). 28/28 static requests 200; the only console error is a `favicon.ico` 404. Highlighting stays unobservable here — `src/main.ts` never reads `lox.highlighted` — and is covered by the transform-level assertions instead. Scratch `.playwright-cli/` evidence removed from `examples/vite-trace-demo/src/`; `git status examples/` clean; dev server stopped. |
| 2026-08-14 13:10 | [Implementation] One review finding left to the user | `ACC-undeclared-doc-edits-in-tree`: deferred at the user's direction — the staged `CHANGELOG.md`, `documentation/environments.md` and `documentation/index.md` will be handled manually later. Left untouched, and the git index was not modified. Until then the changelog still advertises the deleted `@trace()` options as current, so it must not be committed with this change as it stands. |
| 2026-08-14 13:28 | [Reviewing] WARN — pass 2, `review-2.md` | 3 agents dispatched (code, test, acceptance; simplicity and perf dropped after clean pass-1 returns, security and a11y N/A). All 7 pass-1 fixes verified landed and correct; no new undeclared scope. 3 findings: 1 HIGH, 1 MEDIUM, 1 LOW; 0 CRITICAL. Queue: 2 🟢 local, 1 🟡 contained (deferred), 0 🔴 redesign. One new finding: `CODE-moduleid-trace-option-dangling` — `src/index.ts:41-43` still names the deleted `moduleId` trace option, and it ships into the TypeDoc page. The HIGH is the carried-over `ACC-undeclared-doc-edits-in-tree`, still deferred to the user's commit-path decision — no code finding above LOW exists. Test lens swept all 39 remaining `@ts-expect-error` rows with no recurrence of the pass-1 defect class. |
| 2026-08-14 13:34 | [Implementation] Review pass 2 fixes | `CODE-moduleid-trace-option-dangling`: fixed — `src/index.ts:41-43` and the same sentence on `ModuleId` at `src/types.ts:183-184` now name the marker's module selectors instead of the deleted `moduleId` trace option. `src/index.ts` names `trace.m(...)` and `trace.<Module>` (the direct members typed through `TraceModuleId`); `src/types.ts` names only `trace.module(...)` / `trace.m(...)`, since those take `ModuleId` while the direct members take the narrower `TraceModuleId`. A sweep of `src/` and `packages/*/src/` found no third site. `ACC-undeclared-doc-edits-in-tree`: still deferred — its fix is a commit-path choice the user owns, and the review directs that it must not be resolved by editing or reverting the three staged documentation files; the git index was not touched. `ACC-no-spec`: still deferred — belongs to the documentation follow-up, per plan stream 7. All five gates exit 0 (`lint` 0 errors, `test` 559 passing across 23 files, `build`, `typecheck:test`, `typecheck:types`). The one lint warning is `src/core/Modules.ts:87` (`newline-before-return`), pre-existing and outside this change. TypeDoc regeneration stays deferred by plan stream 7, so `docs/` still carries the old sentence until the documentation follow-up runs `pnpm run docs`. |
| 2026-08-14 16:59 | [Reviewing] WARN — pass 3, `review-3.md` | 2 agents dispatched (code, acceptance; test dropped — pass 2 verified its only finding fixed and swept all 39 remaining `@ts-expect-error` rows, and no test file changed in the fix round; simplicity and perf dropped after clean pass-1 returns; security and a11y N/A). **No new findings.** `CODE-moduleid-trace-option-dangling`: verified fixed at both `src/index.ts:40-43` and `src/types.ts:183-184`, with the `TraceModuleId` vs `ModuleId` split checked against the types and no third site anywhere in `src/` or `packages/*/src/`. Queue: 1 🟢 local, 1 🟡 contained, 0 🔴 redesign — none actionable by `implement`. The WARN comes solely from the carried-over `ACC-undeclared-doc-edits-in-tree`, still deferred to the user's commit-path decision. Both lenses independently reproduced the straggler sweep and the built-tree export check; build freshness for the pass-2 fix was corroborated by `dist/*.d.ts` mtimes 24s after the `src` edits. Three passes have now produced no open defect in the change itself. |
| 2026-08-14 17:15 | [Finalization] Changelog | Reconciled `[Unreleased]` in `CHANGELOG.md` against the 2.0.0 baseline: added 1 `Removed` entry (**Breaking:** the `@trace` / `@initLoxer` removal, with migration paths and the `loxer/trace` import move), rewrote 6 entries that named deleted decorator options (`moduleId` trace option, `@initLoxer()` registry pin, `argsAsItem` / `resultAsItem` rename clause, `argsAsProps`, `resultAsProps` / `printResult`, the `level` trace option), folded per-side `.h('open')` / `.h('close')` into the marker's fluent-configuration entry, and removed 4 entries describing decorator-only capability users never received (TC39 decorator protocol support, `printArgs` / `printResult`, and three `@trace` fixes). Implies **MAJOR** → 3.0.0: a `Removed` from the public API. No manifest touched. |
| 2026-08-14 17:15 | [Finalization] Commit | [2026-08-14-markeronlytracing] Remove the trace and initLoxer decorators |
