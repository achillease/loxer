# Worklog — 2026-08-19-columnfreeboxes

> **How to log:** Add each new row at the bottom of this table. Use a real `YYYY-MM-DD HH:MM` time (`Get-Date -Format "yyyy-MM-dd HH:mm"` / `date "+%Y-%m-%d %H:%M"`). Start **Action** with the phase, for example `[Planning]` or `[Implementation]`.

| Timestamp | Action | Detail |
|-----------|--------|--------|
| 2026-08-19 01:11 | [Planning] Plan written | Column-free boxes: `nc()`/`noColumn()` stamps `columnFree` on `Lox` (not `OutputLox` — `findOpenLox` returns a raw `Lox` pre-init), one `&&` in `Loxes.addOpenLox` suppresses the column, one close-side branch in `BoxFactory.getOfLogBox`, and a ten-point mirror on the Babel marker chain. Grounded by an architect technical survey; ordering, `removeCorrespondingOpenLox`, `getOpenLoxes` and the `modifierKey` fallback verified directly. |
| 2026-08-19 01:11 | [Planning] Two decisions settled | Name is `nc`/`noColumn`, not `noBox` — the box persists and `boxLayoutStyle: 'off'` already means "box off" with opposite behavior. `ErrorLox.openLoxes` will include column-free boxes, so `Loxes.getOpenLoxes` is rewritten to derive from `_loxes` (defined + type `open` + `!hidden`) instead of `_openLogBuffer`. |
| 2026-08-19 01:22 | [Implementation] Streams 1–5 built | `columnFree` on `LoxInit`/`Lox` (constructor default `false`), read off the chain in `openAtLevel` and written as a literal at that `new Lox({...})`; inherited by destructuring the open lox in `appendToOpenLox`; resolved for errors on `errorLox` in `toErrorLox` via `findOpenLox(lox.id)`, before the box is built. One `&& !lox.columnFree` in `Loxes.addOpenLox`; `getOpenLoxes` rewritten to derive from `_loxes` (defined + `type === 'open'` + `!hidden`). One close-side branch in `BoxFactory.getOfLogBox`; `getOpenLogBox` untouched, with the unreachable-`break` trap recorded in a comment. `nc`/`noColumn` on `Modifiers<Delete>` with the `nc` phantom alias, backing `_columnFree`, and the `resetState` line. Marker chain mirrored across all ten registration points. |
| 2026-08-19 01:22 | [Implementation] Two implementation calls | `toErrorLox` assigns `errorLox.columnFree`, not the incoming `lox` — that matches how `hidden`/`module`/`box` are set there and leaves the queued `Lox` unmutated on the pre-init replay path; it lands before `errorLox.box`, which reads the flag. `nc`/`noColumn` are filtered *out* of `reservedPointDirectModules`, mirroring how `props` is excluded: the point chain has no such modifier, so a module named `nc` stays directly selectable there. `trace.point.nc()` is still rejected — its own inline allowlist reports "does not support fluent member". |
| 2026-08-19 01:22 | [Implementation] Verified against `dist/`, not `src/` | Resolved the plan's open question: no `playground/*.js` change — scratchpad scripts importing `dist/index.js` hosted the gate instead, keeping the repo free of example churn. Reproduced the spec's second capture byte for byte (depth pins at 1). Confirmed `columnFree` on all seven `.of(id)` entry points; `│─` members and errors; `│╰→` closes carrying time; a box nested inside sitting at depth 0; hidden-wins outputting nothing; `getOpenLoxes` excluding hidden while including column-free, in chronological order; no chain leak into the next box; `nc(false)` reserving a column; `.of(id)` after close and a direct error both `false`; props connecting at marker depth 0. The trace mirror ran end to end through `packages/babel-plugin-loxer-trace/dist` against `dist/trace.js` — a three-frame stack pinned at depth 1 — and each build-time rejection fired with its own message: double-chain, reserved direct module, arity, and the point chain. Double-chaining pinned as a compile error against the `dist/` typings. |
| 2026-08-19 01:22 | [Implementation] Gates green, Streams 6–7 not started | `pnpm build`, `pnpm lint`, `pnpm test` (562 passed, 23 files, no suite edited), `pnpm typecheck:test`, `pnpm typecheck:types` all exit 0. Stream 6 (test suites) belongs to the Testing phase and Stream 7 (the two guides plus `pnpm run docs`) to the Documentation phase, so neither ran here. JSDoc on the modifier pair, the marker-chain pair and `Lox.columnFree` is written — it ships with the code and is what `pnpm run docs` will pick up. The silent-propagation risk the plan names as top is still unpinned by a suite — it is verified by hand above, which a regression would not repeat. |
| 2026-08-20 00:20 | [Planning] Scope extended to the built-in console output | Folded a second concern into the spec and plan: how the built-in development console marks a highlighted log and a log's severity. Diagnosis that produced it — `Loxer.h()` emitted ANSI `7` (reverse video), which a Chromium devtools console does not implement, so highlighting was invisible in every Electron/Vite app; and the highlight was painted onto the message where the severity prefix already lives, so `h().warn()` showed orange and no highlight at all (a known effect recorded in the 2026-07-28 worklog). Spec retitled to "Column-free boxes and the default console output" with a second frame, its own in/out of scope, six acceptance criteria and four definition-of-done items; plan gained Stream 8 plus its test list, two critical files, four risks and a `dist/` verification paragraph. Filenames unchanged (`documentation/specs/column-free-boxes.md`, this folder) so existing references still resolve. |
| 2026-08-20 00:20 | [Planning] Third decision settled | The highlight marks the **module column**, not the message, and its default is an explicit background (grey `rgb(70,70,70)`) rather than reverse video. Severity keeps the message, highlighting keeps the module column, so the two compose instead of one overwriting the other. Merging both into one message prefix was rejected: a close line needs its own foreground too, so the merge would have to be re-derived per level. |
| 2026-08-20 00:20 | [Implementation] Stream 8 built | `ANSIFormat.highlightPrefix` returns `colorBackground(70, 70, 70)` where no `highlightColor` is configured (the configured branch is untouched, so a destination naming a color keeps it, and `colorHighlight` inherits the default); `colorLox` drops the highlight arm from the message prefix chain and wraps `moduleText` instead. `OutputStreams.devErrorOut` writes with `console.error`; `devLogOut` writes with `console[outputLox.level]` and prefixes two spaces for every level but `'warn'`, aligning rows Chromium gives no icon with rows it does. Both `prod` paths and both `_output`-present branches untouched. |
| 2026-08-20 00:20 | [Implementation] Verified against `dist/` | Probe through the rebuilt `dist/index.js` with one module registered and no callback: a highlighted log's module column carries `48;2;70;70;70`; a highlighted warn keeps `38;2;255;165;15` on the message, a highlighted close `38;2;180;255;180`, a highlighted error its red badge plus red message — each beside the marked column; rows arrive on `console.info` / `console.warn` / `console.debug` / `console.error` with the timestamp at a stable column. Two effects found and recorded as spec open questions rather than fixed: a log with no module (`NONE` → empty `slicedName`) shows no mark at all, which includes `init()`'s own "Loxer initialized" line; and a highlighted close renders black module text (`endTitleOpacity` `0`) on the grey highlight. Not yet checked in a devtools console, which is the destination the change exists for. |
| 2026-08-20 00:20 | [Implementation] Gates: 8 test failures, all expectation drift | `pnpm build` and `pnpm lint` exit 0. `pnpm test` is 554 passed / 8 failed across 3 files, and every failure pins a behavior Stream 8 deliberately changed: five in `test/format.test.ts` assert the highlight on `colorLox(...).message` and the reverse-video default (`:32`, `:43`, `:64`, `:230`, `:253`), and `test/initialization.test.ts:363` plus `test/trace-message-console.test.ts:39,74` mock `console.log` and now capture nothing because their logs route to `console.info`. No product defect among them. Rewriting these belongs to the Testing phase together with Stream 6, which is still not started. |
| 2026-08-20 00:46 | [Reviewing] WARN - pass 1, `review.md` | 5 agents dispatched (code, simplicity, perf, acceptance, test); a11y and security skipped as not applicable. 15 findings: 5 HIGH, 8 MEDIUM, 2 LOW, no CRITICAL, no redesign. Queue: 5 local, 6 contained for `implement`, 4 routed to Testing/Documentation. Top finding `CODE-highlight-still-on-message` - Stream 8 added the highlight to the module column but never removed it from the message prefix chain, so a highlighted log is marked twice and a highlighted close line loses its green; three lenses found it independently and it was verified directly in the file. The eight failing `pnpm test` expectations are not all drift: the `format.test.ts` message assertions are satisfiable under that defect, so rewriting them against current output would pin the bug. Also unrecorded: `OutputRenderer`'s `endTitleOpacity` default moved `0` to `0.4` for every close line in every colored destination, which no stream, spec bullet, or worklog row covers. |
| 2026-08-20 01:23 | [Testing] One product fix applied inline | `CODE-highlight-still-on-message` (review pass 1, HIGH, 🟢 local) fixed before any test was written: deleted the `if (lox.highlighted)` arm from `ANSIFormat.colorLox`'s message prefix chain so it starts at `if (lox.type === 'close')`. The module-column wrap already carried the highlight, so a highlighted log was marked twice and — because the arm shadowed the `else if` beneath it — a highlighted close line lost its green entirely. Fixed here rather than deferred because `ACC-gates-red` makes it a precondition for this phase: the five `format.test.ts` message expectations are satisfiable under the defect, so rewriting them first would have pinned the bug. No other product change; the ten remaining review findings stay routed as written. |
| 2026-08-20 01:23 | [Testing] Stream 6 + Stream 8 suites written | Closes `TEST-columnfree-zero-coverage`. `test/boxed.test.ts`: column-free open/member/close lines, time consumption on a column-free close, nested-inside depth, hidden-wins precedence, a mixed column/column-free run, and `getOpenLoxes` including a column-free box while still excluding a hidden one in chronological order. The plan's top risk — a missed `columnFree` propagation renders byte-identical output — is pinned by a `test.each` over `add`/`warn`/`info`/`debug`/`close` plus an error case asserting `[true, true, false, false]` across `error`/`namedError`/after-close/direct, covering all seven `.of(id)` entry points on the flag itself. `test/unboxed.test.ts`: the unguarded `resetState` leak, and `nc(false)`. `test/format.test.ts`: the five broken highlight expectations rewritten onto `moduleText` with `message` pinned to bare `'Lox2!'`, a configured `highlightColor` override, a warn/close composition table, and a direct `BoxFactory.getOfLogBox` drive of the new close branch (`╰→ `, which is `→ ` without it). `test/initialization.test.ts`: a per-level console-method + padding table. `test/trace-message-console.test.ts`: mocks moved to `console.info`. `test/plain-function-trace-core.test.ts` and `test/trace-point.test.ts`: the marker-chain mirror and its point-chain rejection. `test/types/registry.test-d.ts`: `@ts-expect-error` double-chain pins plus any-order composition. |
| 2026-08-20 01:23 | [Testing] Built-tree gate made durable | Closes the first half of `TEST-built-tree-gate-not-durable`. The `dist/` verification moved out of scratchpad scripts into `test/dist-consumer.test.ts`, which is the committed home for it: an `nc()` case asserting `box.length` stays at 2 for a normal box nested inside a column-free one, and a console-fallback case asserting the grey module-column background plus all four level-named methods against the rebuilt tree. |
| 2026-08-20 01:23 | [Testing] Three findings deliberately left unpinned as `test.todo` | `CODE-props-indentation-off-by-level-indent`, `CODE-error-level-log-double-indented` and `CODE-endtitleopacity-default-changed` are 🟡 contained and routed to `implement`; the last needs a spec call on whether `0.4` stays. Registering them as `test.todo` naming the finding id keeps the required coverage recorded without a red suite or a silent product change. Close-line tests pass an explicit `endTitleOpacity` so no expectation depends on the contested default. The per-level routing table covers `warn`/`info`/`debug`, which route correctly; the error-level ordinary log is the todo. |
| 2026-08-20 01:23 | [Testing] E2E skipped, and the devtools route does not work as written | No Playwright anywhere in the repo and no UI in a logging library, so no browser spec was written. More importantly, `TEST-built-tree-gate-not-durable`'s second half cannot be closed through `pnpm demo`: `examples/vite-trace-demo` registers `output: record`, and `OutputStreams` forwards the raw lox and never touches the console when `_output` is present — so the demo bypasses the entire Stream 8 branch. Confirming the highlight in a Chromium devtools console needs a page that inits Loxer with no `output` callback. Left open, not claimed. |
| 2026-08-20 01:23 | [Testing] Gates green | Verified independently, not on agent report: `pnpm build` 0, `pnpm test` **598 passed / 3 todo / 0 failed** across 23 files (from 554 passed / 8 failed), `pnpm lint` 0, `pnpm typecheck:test` 0, `pnpm typecheck:types` 0. `ANSIFormat.ts` and `Loxes.ts` both reach 100% statement coverage. No new product bug was found, so no `test-bugs.md` was created. Definition-of-done item 2 is now met; items 6, 7 (second half) and 12 remain Documentation's. |
| 2026-08-20 01:36 | [Reviewing] Pass 1 dispositions | `CODE-highlight-still-on-message`: **fixed** - the `lox.highlighted` arm was removed from `colorLox`'s message prefix chain in the tree after the pass was written, so the highlight now marks the module column only and a close line's green is reached unconditionally. The plan's Stream 8 text and spec criteria H-1/H-3/H-6 already describe exactly this, so neither was adjusted; adjusting them would have made them wrong. Settled alongside it: a close line keeps its own color, the same precedence the level arms already had over a mark. `CODE-endtitleopacity-default-changed`: **accepted, not reverted** - `0.4` is the module color at 40% channel brightness, a darkened shade of the title rather than the black `0` produced; `src/types.ts` JSDoc rewritten to document the multiplier and the new default. `TEST-columnfree-zero-coverage` and `ACC-gates-red`: **superseded** - ten test suites were modified at 01:12-01:20, after the pass; `pnpm test` is now 23 files / 598 passed / 3 todo, and the seven suites referencing `columnFree` include the `.of(id)` propagation assertions the plan named as its top risk. `CODE-node-stderr-routing`: **deferred to debt** as `D-5` in `documentation/debt.md`, index row included. Still open for the next pass: `CODE-props-indentation-off-by-level-indent`, `PERF-getopenloxes-unbounded-scan`, `CODE-error-level-log-double-indented`, `CODE-highlightcolor-jsdoc-stale`, `SIMP-modtext-reimplements-colorhighlight`, `SIMP-errorlox-columnfree-relookup`, `SIMP-highlight-default-magic-rgb`, `ACC-docs-not-written`, `CODE-testing-rule-console-log-stale`, `TEST-built-tree-gate-not-durable` (`test/dist-consumer.test.ts` is modified in that same batch - next pass confirms whether it covers the two gates). |
| 2026-08-20 01:44 | [Reviewing] WARN - pass 2, `review-2.md` | 3 agents dispatched (code, acceptance, test); simplicity and perf skipped under the pass-2 three-lens cap, with their four open findings re-verified unchanged directly in the tree; a11y and security not applicable. 13 findings: 3 HIGH, 6 MEDIUM, 4 LOW, no CRITICAL, no redesign. Queue: 7 local, 6 contained. Pass-1 `CODE-highlight-still-on-message` confirmed fixed and `TEST-columnfree-zero-coverage` confirmed superseded. New top finding `CODE-highlight-invisible-without-module` - the pass-1 fix exposed it: module `NONE` has an empty `slicedName`, so a highlighted log without `.m(...)` wraps zero characters and marks nothing, including `init()`s own line. It is a terminal regression, since reverse video on the message used to render there, and the new test at `initialization.test.ts:419` asserts `toContain` on the escape, which holds while the escape wraps nothing. Needs spec open question 4 answered before it can be implemented. Also new: `ACC-endtitleopacity-spec-unamended` (the `0.4` default was accepted in code but the spec still carries it as open, and criterion H-6 now contradicts it) and `CODE-moduleopacity-jsdoc-diverged`. `TEST-built-tree-gate-not-durable` half closed - the `dist/` cases are real; the Chromium devtools check still cannot run through `pnpm demo`. |
| 2026-08-20 19:23 | [Implementation] Review pass 2 fixes | Ten of eleven selected findings fixed; all five gates green (598 passed / 2 todo / 0 failed). `CODE-highlight-invisible-without-module: fixed` - the highlight now marks the **time field** exclusively, per the user decision on spec open question 4. The module column could not hold it, since module `NONE` renders as an empty string and a mark there wrapped zero characters. A marked time field drops `fgTime` rather than composing with it: `fgTime` is `rgb(70,70,70)`, the very grey the highlight defaults to, so composing them would have printed grey text on its own grey background - invisible. `CODE-props-indentation-off-by-level-indent: fixed` via one `getLevelIndentation` helper that both the console line and `getPropsIndentation` read, which is also `CODE-error-level-log-double-indented: fixed` (`console.error` draws an icon exactly like `console.warn`, so an error-level ordinary log is no longer padded). `PERF-getopenloxes-unbounded-scan: fixed` - `removeCorrespondingOpenLox` deletes the key instead of tombstoning it, so `getOpenLoxes` walks open boxes rather than every id ever issued. `SIMP-errorlox-columnfree-relookup: fixed` - `columnFree` is threaded through `internalError` from the open lox the `of()` closures already hold, and `toErrorLox` drops its second `findOpenLox`. `SIMP-modtext-reimplements-colorhighlight: fixed` (the time mark calls `colorHighlight`), `SIMP-highlight-default-magic-rgb: fixed` (`DEFAULT_HIGHLIGHT_COLOR = #464646`, which collapses `highlightPrefix` to one branch), `CODE-highlightcolor-jsdoc-stale: fixed`, `CODE-moduleopacity-jsdoc-diverged: fixed`. `ACC-endtitleopacity-spec-unamended: fixed` - `endTitleOpacity` reverted to its documented `0` default, because the only reason for `0.4` was black module text on the grey highlight and the highlight no longer touches the module column; the spec now records both open questions 4 and 5 as settled, and question 5 as moot. `CODE-testing-rule-console-log-stale: deferred` - `rules/testing.md` is project guidance the Documentation phase owns. |
| 2026-08-20 19:23 | [Implementation] Seven expectation drifts repointed, two todos retired | The decision invalidated every assertion pinning the grey background on `template.module`, so seven failures were repointed at the time field rather than deleted: five in `test/format.test.ts` (helpers read the timestamp off the lox so no clock is hardcoded), and two in `test/initialization.test.ts` - the highlight test, plus the structured-renderer test whose own template now passes the padded props indentation the console really uses. The `dist/` consumer case was tightened too: it asserted only that the line contained the grey somewhere, which stayed true through the move, so it now pins the highlight as the line opener and asserts the two greys never compose. Two `test.todo` entries were retired as implemented, and the two left standing were rewritten to name what is still owed rather than defects that are now fixed: an `endTitleOpacity`-default pin through `ColoredOutputLoxRenderer`, and the error-level pad, which `Loxer[level]()` cannot reach because `Loxer.error()` is the error method and routes to `devErrorOut` - it needs a box opened at level `error` through the trace opener. |
| 2026-08-20 19:31 | [Documentation] Guides and the test rule closed | `ACC-docs-not-written: fixed`. `documentation/logging.md` teaches `nc()`/`noColumn()` in its manual-box section (column-free semantics, one-shot chaining, a hiding threshold still winning, `OutputLox.columnFree` for a custom destination) and teaches `h()`/`highlight()` under output as a mark on the time field, with how severity and highlighting compose and how the console dispatches per level. Both Node consequences a reader needs are stated there: `console.warn` on stderr, linked to the `D-5` register entry rather than duplicated, and Chromium hiding `console.debug` without Verbose. `documentation/tracing.md` gained `noColumn`/`nc` in its modifier enumeration plus the marker-chain form, and `README.md` names a column-free box among the traced capabilities - both enumerations had listed only `module`/`m`, `highlight`/`h` and `printProps`/`pp`. `CODE-testing-rule-console-log-stale: fixed` - the `PropsPrinter` bullet in `rules/testing.md` carried three stale facts, not one: `console.log` where dispatch is `console[level]`, a `disableColors` config that does not exist anywhere in `src/`, and the pre-move path `src/core/OutputStreams.ts`. It now names the per-level methods, points at the suites pinning the dispatch, and describes how a test actually gets uncolored output. `pnpm run docs` regenerated `docs/` - typedoc printed "html generated at ./docs" and 13 files changed. Its one warning is that the private return-type alias `nc` is not itself a documented symbol, which its three sibling modifier aliases share; `noColumn` and `columnFree` are both present in the generated reference. |
| 2026-08-20 20:07 | [Implementation] endTitleOpacity stays 0.4 | Reverted the earlier revert on the user decision, restoring the accepted default. `OutputRenderer` darkens a close line module title with `?? 0.4`, and the `endTitleOpacity` JSDoc documents that value and what it means - a multiplier on the module colors channels, `0.4` being a darkened shade of the module own color. The spec now carries the decision rather than the question: a Design paragraph states the darkened close title and that the option reaches every colored destination through the exported renderers, In scope names the default, and open question 5 records it as settled and applying to every close line alike, not only a highlighted one. Criterion H-6 is unaffected: the default applies whether or not a log is highlighted, so it is not a highlight-dependent difference. The owed `test.todo` in `test/format.test.ts` names `0.4` as the value to pin through `ColoredOutputLoxRenderer`. All five gates exit 0 (598 passed / 2 todo), and `pnpm run docs` regenerated `docs/` with the new default published on the renderer-options page. |
| 2026-08-20 22:50 | [Reviewing] PASS - pass 3, `review-3.md` | 3 agents dispatched (code, acceptance, test); simplicity and perf skipped under the pass-2+ three-lens cap, and unlike pass 2 they had no open finding left to re-verify - all five were dispositioned `fixed` and the code lens confirmed each remediation in the tree. 12 findings: 6 MEDIUM, 6 LOW, no HIGH, no CRITICAL, no redesign - the first pass with nothing above MEDIUM. Queue: 6 local, 5 contained, plus one Documentation item. Every pass-2 remediation verified in the tree rather than taken on the worklog: the highlight is on the time field only, `getLevelIndentation` is correct for all four levels, `delete` in `removeCorrespondingOpenLox` is safe and keeps ordering, and all nine `internalError` call sites pass the right `columnFree`. What the remediation left behind: `CODE-highlightcolor-empty-string-throws` - collapsing `highlightPrefix` to one branch dropped the falsy guard, so `highlightColor: ''` now throws `Color()` from inside rendering while its sibling `colorizePrefix` still guards it; `CODE-highlight-modifier-jsdoc-stale` - the `highlight()` JSDoc still says the log colors are inverted, and it is published in the regenerated `docs/`; `CODE-tracepoint-columnfree-not-inherited` - a `trace.point` log carries the box id but never the flag. Also new: `TEST-props-nc-depth-vacuous` (the column-free props test passes with `.nc()` deleted) and four LOW spec/guide items. `TEST-built-tree-gate-not-durable` still half open - it is the premise H-2 and H-5 rest on, and `pnpm demo` still cannot reach the console fallback. |
| 2026-08-20 23:15 | [Implementation] Review pass 3 product fixes | `CODE-highlightcolor-empty-string-throws: fixed` - `highlightPrefix` takes `color?: string` and falls back through `isNES`, the repo's own non-empty-string gate, so `highlightColor: ''` renders the default grey instead of throwing `Color()` out of a destination; verified against the rebuilt `dist/`, where `colorHighlight('X', '')` and `colorHighlight('X')` are now byte-identical and a named color still wins. `CODE-tracepoint-columnfree-not-inherited: fixed` - `writeTracePoint` resolves the containing open lox once and reads both things a point inherits off it, the module and `columnFree`, replacing a lookup that ran only when the point named no module; a point inside a column-free box now reports `true`, confirmed through `dist/trace.js`'s `__tracePoint`. `CODE-levelindent-not-shared-with-error-line: fixed` - `devErrorOut` passes `getLevelIndentation(errorLox)` into both the line and `getPropsIndentation`, so the helper's "one place on purpose" claim holds; output is byte-identical, since `internalError` stamps `level: 'error'` and the predicate maps that to no pad. `CODE-highlight-modifier-jsdoc-stale: fixed` - `Modifiers.highlight`'s JSDoc named inverted foreground/background; it now names the marked time field and the grey default, and states that the message keeps its severity color. The neighbouring bullet about a highlighted error appending its stack stays - `OutputRenderer` still gates the stack on `lox.highlighted`. `CODE-colorhighlight-default-changes-props-keys: fixed as documented, behavior kept` - the shared default is deliberate, so `colorHighlight`'s JSDoc now states it and names the second consumer: `PropsPrinter` passes `colorHighlight` to `colorize`, which calls it with the text alone, so a `keys`-selected props key always takes the default grey. |
| 2026-08-20 23:15 | [Implementation] Console mode for the demo | Unblocks `TEST-built-tree-gate-not-durable`'s second half. `examples/vite-trace-demo` opened as `?console` inits with no `output` callback, which is the only route that reaches Loxer's built-in console rendering, and writes one preview burst on load: a row per level, a highlighted log, a highlighted warn, a highlighted log with no module, a column-free box beside a column-reserving one, a highlighted close, and an error. The page links to the mode and says where output goes. `tsc --noEmit && vite build` for the demo exits 0. The manual observation itself is still owed and is inherently a human step - Playwright reads console text, not what a devtools console draws - so it is not claimed here. |
| 2026-08-20 23:31 | [Testing] Both `test.todo` entries retired, one vacuous test replaced | `pnpm test` is now **606 passed / 0 todo** across 23 files, up from 598 passed / 2 todo, and `pnpm typecheck:test` exits 0. `TEST-props-nc-depth-vacuous: fixed` - the old case opened an `nc()` box with nothing else open, where a column-reserving open produces the identical box, so `.nc()` was deletable without failing. It is replaced by a contrast: two nested normal boxes connect props at marker depth 2, the same nesting with a column-free inner box at 1, and the shallower depth is then fed back through `PropsPrinter.of(...).print(...)` to prove it is where the block actually connects. `ACC-errorlevel-pad-unpinned: fixed` - the error-level *ordinary* log is pinned through `__openTrace('error', ...)`, the only route that reaches `devLogOut` with that level, asserting `console.error` and no pad. `ACC-endtitleopacity-spec-unamended` (test half): fixed - a `test.each` pins the `0.4` default and a destination-configured `0.7` through `ColoredOutputLoxRenderer`, plus that an open or single line is never darkened. The default pin was mutation-checked, not trusted: flipping `?? 0.4` to `?? 0` in `OutputRenderer` fails exactly that row, and only that row. |
| 2026-08-20 23:31 | [Testing] The three pass-3 product fixes pinned | `highlightColor: ''` now has both a unit pin (`colorHighlight('X', '')` equals the no-color call, a named color still wins) and a rendering pin (a highlighted log renders instead of throwing when a destination configures the empty string). The trace-point inheritance is pinned in `test/trace-point.test.ts` across all three cases - column-free box, normal box, no containing box - driven through `__tracePoint` with the numeric id off `Loxer.open().id`, and asserting the module inheritance that shares the same lookup still holds. `devErrorOut`'s shared `getLevelIndentation` is pinned on the observable contract rather than the formula: an error row starts at the same column as a `warn` row, and its props hang from the column its own message starts at, with the expected width derived from the plain rendered fields instead of re-deriving the internal constant. One stale comment corrected while there: a `format.test.ts` case still said the highlight lives on the module column. No product bug was found, so no `test-bugs.md`. No E2E: there is no Playwright in the repo and no UI in a logging library - the demo's console mode is a manual devtools route, not an automatable one. |
| 2026-08-20 23:37 | [Documentation] Spec amended, guide voice corrected, three rule gaps closed | `ACC-spec-naming-provisional: fixed` - the selection-surface example called `.nb()`, a method that never existed; it now calls `.nc()`, the Naming section states the decided pair, and open questions 1-3 are struck as settled in the style questions 4 and 5 already used. `ACC-endtitleopacity-spec-unamended` (spec half): fixed - a criterion now covers the `0.4` default, states that it reaches every destination calling the exported colored renderers, and that it applies whether or not a log is highlighted; the out-of-scope bullet no longer claims both changes are confined to the built-in destination, which contradicted it. `ACC-dod-playground-substituted: fixed` - definition-of-done item 5 names `test/dist-consumer.test.ts`, the committed home the gate actually lives in, instead of the `playground/*.js` script that was deliberately not built. A new definition-of-done item gates the Chromium devtools observation and names the `?console` route that serves it - the gap that had no item at all. `ACC-guide-rejected-design-rationale: fixed` - `documentation/logging.md` said the time field carries the mark because marking the module would show nothing on calls that skip `.m(...)`, which argues against the rejected design; it now states only that every log has a time field. The same section no longer routes a reader to the `D-5` register entry: it states the stderr consequence and the `output` callback that resolves it. Three rule-coverage gaps closed in `src/core/AGENTS.md`: the props-indentation invariant now counts the level pad and requires both console lines to derive it from the one predicate; a new bullet states that the console fallback requires all four `LogLevel` methods on the host `console` and that stream routing is the host's business; and another states that `_loxes` holds only open boxes, that closing deletes the key rather than tombstoning it, and what depends on that. `pnpm run docs` regenerated `docs/` - typedoc printed "html generated at ./docs", 14 files changed, and the `Modifiers` page now publishes the marked time field with no remaining mention of inverted colors. All five gates exit 0 (606 passed / 0 todo). |
| 2026-08-20 23:48 | [Testing] Devtools observation confirmed by the user | `TEST-built-tree-gate-not-durable: fixed` - both halves are now closed. The user ran the demo's `?console` route and reported the rendering fine, which is the one check no gate in this repo can make: a Node suite asserts escape sequences, and Playwright reads console text rather than what a devtools console draws. That settles the two premises the highlighting half rests on - that an explicit background renders where reverse video did not, and that the two-space pad matches the icon shift, so every timestamp starts at the same column. Recorded as the user's observation, not a reproduced gate; a future change to `ANSIFormat`'s marks or `OutputStreams`'s padding needs the same manual pass again, and the `?console` route is the committed way in. |
| 2026-08-21 01:53 | [Reviewing] PASS - pass 4, `review-4.md` | 1 agent dispatched (code); simplicity, perf, acceptance and test skipped because every pass-3 finding has a fixed disposition, and a11y/security were not applicable. The targeted current `HEAD` diff was judged with no findings and no rule gaps. Queue: 0 local, 0 contained, 0 redesign. No tests or apps ran; the orchestrator independently checked the same hunks and `git diff --check` reported no whitespace errors. |
| 2026-08-22 22:52 | [Finalization] Changelog | Added 3 entries under `[Unreleased]` in `CHANGELOG.md` and rewrote 2 existing entries to describe the final trace-marker and renderer behavior. The accumulated release delta from `v2.0.0` implies MAJOR (`3.0.0`) under Semantic Versioning. |
| 2026-08-22 22:55 | [Finalization] Commit | [2026-08-19-columnfreeboxes] Add column-free boxes |
