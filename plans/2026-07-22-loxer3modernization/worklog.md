# Worklog — 2026-07-22-loxer3modernization

> **How to log:** read this file first, then add your row at the **bottom** of the table, below the last existing row (newest last — never insert after the header). The **Timestamp** is `YYYY-MM-DD HH:MM` from a real clock (`Get-Date -Format "yyyy-MM-dd HH:mm"` / `date "+%Y-%m-%d %H:%M"`) — never guess the time. Prefix each **Action** with the workflow phase in brackets — e.g. `[Planning]`, `[Implementation]`, `[Testing]`, `[Reviewing]`, `[Documentation]`, `[Finalization]` — so every entry is traceable to the phase that wrote it.

| Timestamp | Action | Detail |
|-----------|--------|--------|
| 2026-07-22 19:03 | [Planning] | Wrote plan.md for loxer 3.0.0 modernization. Grounded by architect (technical survey) + web-researcher (selection gate). Decided: dual ESM+CJS via tsc-only 3-tsconfig split; vendor MIT color parser (color-string 1.6.0 + color-name 1.1.4 + color-convert 1.9.3) into src/core/color/, drop `color`/`@types/color`; target ES2022, drop tslib; toolchain to eslint 10 flat config, TS ~6.0, prettier 3, typedoc 0.28 (drop neo-theme), husky 9; engines.node >=20 (EOL caveat noted), @types/node ^20; publish safety via --provenance + pnpm audit + committed lockfile. |
| 2026-07-22 19:15 | [Implementation] | Slice 1 done: vendored MIT color parser into src/core/color/ (colorNames.ts, parseColor.ts, index.ts) from color-string@1.6.0 + color-name@1.1.4 + color-convert@1.9.3 with copyright headers + array-copy fix; rewrote ANSIFormat.ts import; dropped `color` + `@types/color` from package.json (dependencies now empty). build+lint+test green (61/61); parity sanity-check passed for hex/named/rgb/hsl/hwb + throw-on-unparseable. |
| 2026-07-22 19:22 | [Implementation] | Slice 2 done: dropped `tslib`; bumped tsconfig target ES5 -> ES2021 (interim; TS 4.4 caps at es2021, ES2022 finalized in toolchain slice). Native class emit now — Error.js has no `__extends`, and `new NamedError() instanceof NamedError/Error` returns true (the intended ES5->modern correction). build+lint+test green (61/61). |
| 2026-07-22 19:23 | [Implementation] | Sequencing deviation from plan's slice order: doing the toolchain upgrade (slice 4: TS ~6.0 + eslint 10 flat + prettier 3 + typescript-eslint 8) BEFORE the dual-build slice (slice 3), because node16 moduleResolution and the ES2022 target require TS >=4.7, and bumping TS forces the lint toolchain to move as a unit (typescript-eslint 4.31 can't parse modern TS; eslint-plugin-prettier 4 incompatible with prettier 3). Same approach + end state; only slice order swapped. typedoc/husky handled after dual build. |
| 2026-07-22 19:38 | [Implementation] | Slice 4 (lint-gate toolchain) done: TS 4.4->6.0.3, eslint 7->10 (flat eslint.config.mjs replacing .eslintrc.json; lint script `eslint .`), @typescript-eslint 4.31 -> typescript-eslint 8 meta-package, prettier 2->3 (.prettierrc.json cleaned: dropped invalid `noSemi`/`rcVerbose` + removed `jsxBracketSameLine`, added explicit `semi:true`), eslint-config-prettier ^10, eslint-plugin-prettier ^5, @types/node 16->20; target finalized ES2021->ES2022. Fixed lint fallout from stricter v8 recommended: `<T extends any>`->`<T>` in Helpers.ts (2x, no-op constraint); re-pointed 2 deliberate `ban-types` disable-comments in Item.ts to their v8 replacements (no-wrapper-object-types, no-unsafe-function-type); removed unused file-level disables in trace.ts + 1 prettier-3 reformat. Added `types:["node"]` to tsconfig (pnpm layout no longer auto-injects `process`). build+lint(exit0)+test(61/61) green. |
| 2026-07-22 19:58 | [Implementation] | Slice 3 (dual ESM+CJS build) done. Added `.js` extensions to all 76+ relative imports in src (scripted; `./color` barrel -> `./color/index.js`; also caught a bare `from '.'` in Modules.ts that nodenext resolution flagged). Split tsconfig into tsconfig.base.json (shared) + tsconfig.cjs.json (module commonjs + node resolution, outDir dist/cjs) + tsconfig.esm.json (module/moduleResolution nodenext, outDir dist/esm); root tsconfig.json now editor-only (nodenext + noEmit). Added scripts/write-pkg-shims.mjs (writes dist/{cjs,esm}/package.json type markers). package.json: version->3.0.0, engines.node >=20, `type:module`, exports map (import/require, types-first) + main/module/types CJS fallbacks, build script runs both tsconfigs + shims. Needed `ignoreDeprecations:"6.0"` in cjs config (TS6 deprecates classic `node` resolution, the only CJS-emit path; TS7-migration touchpoint). Verified: build emits both trees+decls+shims; `require('loxer')`+`import 'loxer'` both self-resolve via exports and run the vendored parser correctly; test(61/61, vitest resolves .js->.ts) + lint(exit0) green. |
| 2026-07-22 20:04 | [Implementation] | Slice 4b done. typedoc 0.21->0.28.20, dropped typedoc-neo-theme; typedoc.json cleaned (removed the neo-theme `theme` path + the `includes:"assets"` option that 0.28 no longer takes). `pnpm docs` exits 0 (default theme). NOTE: typedoc `out:"docs"` co-locates with the workflow's docs/plans/ folder; TypeDoc 0.28 did NOT wipe docs/plans on this run (plan.md+worklog.md intact) but this is a latent layout tension to flag. husky 7->9.1.7: removed dead package.json v4 `husky` key, created .husky/pre-commit (`pnpm lint`), prepare -> `husky && pnpm build`; `pnpm prepare` wired core.hooksPath=.husky/_ (pre-commit now actually functional — it never was before). Full gate green: build+lint(exit0)+test(61/61)+docs(exit0). |
| 2026-07-22 20:12 | [Implementation] | Slice 5 (publish safety) done. `pnpm audit --prod` clean (zero runtime deps). npm pack verify: tarball had two orphan `.tsbuildinfo` (~47KB each) from the incremental dual build -> relocated tsBuildInfoFile out of dist to node_modules/.cache/loxer/{cjs,esm}.tsbuildinfo; tarball 79.8kB->54.4kB, README+LICENSE present, no build cache. publish.yml: added `permissions: id-token: write` (OIDC), `pnpm audit --prod` gate, `pnpm publish --provenance` + NPM_CONFIG_PROVENANCE, node 20->22. main.yml: node matrix 18/20/22 -> 20/22/24 (18 is EOL and below eslint 10's runtime floor), fixed stale Jest flags on test step (`--ci --maxWorkers=2` -> `pnpm test`), added `pnpm audit --prod` step. coverage.yml already vitest-aware, left as-is. Lockfile tracked+in sync. ALL SLICES COMPLETE: final gate build+lint+test(61/61)+docs+audit all green; dependencies:{} engines.node>=20. |
| 2026-07-22 20:30 | [Implementation] | Decision reversal (user): dropped CommonJS support -> ESM-only. Collapsed the dual build: deleted tsconfig.base/cjs/esm.json + scripts/write-pkg-shims.mjs (+ empty scripts/), rewrote a single tsconfig.json (nodenext, target ES2022, outDir dist, tsBuildInfoFile under node_modules/.cache). package.json: build back to plain `tsc`, watch `tsc --watch`, main/types -> ./dist/index.js|.d.ts, exports root-only `{types,default}` (removed `require` + `module` fields). Kept the `.js` import extensions (still required by nodenext). plan.md updated in place (struck the dual decision, revised approach step 3, retired the moot CJS/ESM live-binding risk, added the ESM-only `require`-on-Node<22 break as headline). Verified: build emits single flat dist ESM tree; `import 'loxer'` works; `require('loxer')` succeeds on Node 25 via require(esm) interop (would need import() on Node 20); lint(exit0)+test(61/61)+docs(exit0) green; tarball 54->46kB, 75 files, no tsbuildinfo. |
| 2026-07-22 22:10 | [Testing] | Wrote tests for the two testable slices of the modernization. NEW test/color.test.ts (25 tests) covering the vendored parser (src/core/color/parseColor.ts): hex #rrggbb/#rgb/#rgba/#rrggbbaa, rgb()/rgba() + clamping + percent, hsl()/hsla(), hwb(), named colors (incl. rebeccapurple) + case-sensitivity (uppercase `RED` -> null, faithful to code), `transparent` -> [0,0,0], null contract for unparseable/empty, Color() drop-in .red/.green/.blue + throw contract, and the array-copy regression guard (shared COLOR_NAMES table not mutated across calls). Appended 2 tests to test/error.test.ts locking the ES5->ES2022 instanceof correction (NamedError instanceof NamedError && instanceof Error, with/without wrapped error). Suite green: 61 -> 88 passed. No E2E (library, no browser UI). Bugs revealed: none — `rgb(50%,0%,100%)` -> 127 (not 128) is an IEEE-754 float artifact inherited verbatim from color-string@1.6.0, not a regression; asserted the real value. No test-bugs.md written (nothing to report). |
| 2026-07-22 22:52 | [Documentation] | Fixed `pnpm docs` landing on the README (npm page) instead of the API reference: added `"readme": "none"` to typedoc.json so the module index becomes index.html (regen shows v3.0.0 + Modules index; the old docs/ was stale at v1.1.1). Regen exits 0 (19 pre-existing @link/unexported-type warnings, cosmetic). Also resolved the docs/plans-wipe hazard: TypeDoc `out:"docs"` + default `cleanOutputDir:true` deletes the whole docs/ tree each run and had been silently removing the untracked plan folders. Per user decision, moved plan folders OUT of docs/ to repo-root `plans/` (both 2026-07-07 and 2026-07-22 folders); verified a full docs regen no longer touches them. Future plans go under `plans/`, not `docs/plans/`. NOTE superseded: user later reverted `readme:"none"` — README landing is the intended design (see 00:25 learn entry). |
| 2026-07-23 00:25 | [Documentation] | /learn sweep: landed 2 durable+general lessons into rules/documentation.md (doc-writer agent hit the session limit mid-run and wrote nothing, so completed the well-specified edits directly). (1) Always: workflow plan folders + worklogs live at repo-root `plans/<date>-<slug>/`, never `docs/plans/` (pnpm docs wipes the whole docs/ tree); strengthened the docs/ Never bullet to name `cleanOutputDir:true`. (2) Never: don't add `readme:"none"`/suppress the README front page — `docs/index.html` is intentionally the rendered README; regenerate rather than changing the landing page (from the user's "I want the README as landing page" correction, which reversed the 22:52 change). Dropped as task-specific: color-parser float artifact + uppercase-RED behavior. worklog-discipline memory already updated earlier this session to point at plans/. |
| 2026-07-23 00:37 | [Reviewing] | Ran run-reviews (5 lenses parallel: code, security, perf, test, acceptance; a11y skipped — no UI). Verdict **WARN** — 0 CRITICAL, 2 HIGH, 6 MEDIUM. HIGH: (1) `docs/plans/2026-07-22-loxer3modernization/{plan,worklog}.md` staged under docs/ (violates rules/documentation.md; pnpm docs will wipe — unstage, keep only plans/ copy); (2) hwbToRgb 6-branch switch has 4 hue segments + ratio>1 branch untested. MEDIUM: per-call regex realloc + unmemoized per-log color re-parse (perf ×2); hsl grayscale / hue-wraparound / hsl+hwb clamp branches untested (test ×3); plan.md Verification section stale (still demands dual cjs/esm build) vs shipped ESM-only (acceptance). Security audit ran clean (prod + full tree, 0 CVEs). 6 rule-coverage gaps reported. All findings are local fixes — no re-plan needed. Details: plans/2026-07-22-loxer3modernization/review.md. |
| 2026-07-23 01:07 | [Implementation] | Fixed all 8 review findings + the below-bar security nit (user: "fix all review issues"). HIGH: (1) removed the duplicate `docs/plans/2026-07-22-loxer3modernization/` folder from index+disk (canonical copy stays at `plans/`); (2) added hwb hue-segment tests (yellow/green/cyan/magenta = cases 1/2/3/5) + a `w+b>1` normalization test. MEDIUM perf: hoisted the 7 regex literals in parseColor.ts to module-level consts (compiled once); added a copy-on-return memo `Map` in `parseColorToRgb` (caches parse work but still returns a fresh array each call, so the array-copy/no-leak contract holds). MEDIUM test: added hsl grayscale (s===0), hsl+hwb negative-hue wraparound, and hsl+hwb out-of-range clamp tests. MEDIUM acceptance: rewrote plan.md Verification section for ESM-only (single dist/, no dual smoke test). Security nit: guarded `getRgb` named-color lookup with `Object.hasOwn` so inherited keys (`constructor`, `toString`) return null, not NaN channels. Gate green: build exit 0, lint exit 0, test 98/98 (was 88; +10). Rule-coverage gaps left for the Documentation phase (not fixes). |
| 2026-07-23 01:12 | [Implementation] | Fixed ESM-only fallout in playground/: all 6 examples used CommonJS `require('../dist')`, which throws `require is not defined` now that root package.json is `"type":"module"` (root .js = ESM). Converted each to `import { … } from '../dist/index.js'` (models the recommended ESM consumer usage; live-bindings also make Speedtest/OrderService's `resetLoxer()`→reuse-`Loxer` pattern correct, unlike a stale CJS destructure). Trimmed items.js's unused BoxLayouts/BoxFactory imports. Verified: playground/items/docs/Logo/OrderService all exit 0 (Logo's stderr is its intentional console.error demo); Speedtest loads + runs its benchmark. |
| 2026-07-23 01:21 | [Documentation] | /learn sweep: landed 1 durable+general lesson via doc-writer → root AGENTS.md "## Layout" gained a `playground/` bullet (runnable examples importing built `../dist/index.js`; excluded from build/lint/test so CI can't catch breakage; run manually + keep in sync with the ESM-only module format — the gap that let the ESM migration silently break all 6 files). Harnesses: Claude Code + Codex (canonical AGENTS.md + CLAUDE.md @import, no new files). Dropped: parseColor fix reasoning (mine, not user feedback), worklog ordering (already in worklog-discipline memory), docs/plans duplicate hazard (already in rules/documentation.md). Note surfaced, not acted on: AGENTS.md line ~4 still says "version 2.0.0" (package.json is 3.0.0). |
| 2026-07-23 01:24 | [Finalization] Commit | [2026-07-22-loxer3modernization] Ship loxer 3.0.0: drop `color` dep, ESM-only, ES2022, toolchain modernization |
